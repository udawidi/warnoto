node_modules/
dist/
.env
.env.local
*.log
.DS_Store
supabase/.temp/

# CSV user real (berisi password polos) untuk scripts/bulk_create_users.mjs —
# jangan pernah commit, cuma template-nya (users.template.csv) yang boleh.
scripts/users.csv
scripts/*.users.csv

# Backup/export hasil generate lokal (ratusan MB, ZIP) — jangan masuk git,
# simpan di luar repo (Drive/eksternal) kalau perlu disimpan jangka panjang.
outputs/
migration-tools/node_modules/

# Vercel CLI (link/deploy artefak lokal) — jangan commit.
.vercel
.env*

# Playwright generated artifacts (screenshots, traces, HTML report).
test-results/
playwright-report/

# Graphify tool output (graph.json + cache, data turunan yang bisa di-generate ulang) — jangan commit.
graphify-out/

__pycache__/

# Spec Kit — dipakai sebagai tool GLOBAL (skill di ~/.claude/skills & ~/.codex/skills,
# CLI di ~/.local/bin/specify), seperti caveman/ponytail. Jangan simpan jejaknya di repo.
.specify/
.claude/skills/speckit-*/
.agents/skills/speckit-*/

# Headroom penanda sesi lokal (spesifik mesin) — jangan commit.
.claude/.headroom_wrap_marker.json

# Laporan generate script fill_sap_photos (artefak, bisa di-generate ulang) — jangan commit.
sap_foto_report.json
