# HANDOFF — WARNOTO

**Vendor aktif terakhir:** Claude (Vendor A) | **Update:** 2026-08-11

## Tujuan / benang merah
WARNOTO = aplikasi gudang PLN (React, Vite 4, Supabase self-host, deploy Vercel). Fokus: penyempurnaan UI bertahap + isolasi multi-UPT review-first, bukan redesign besar.

## Keputusan arsitektur (mengikat)

### Infrastruktur & data
- **Production = Supabase SELF-HOST** di `minipc-gudang` (domain `warnoto.com`), migrasi dari Cloud (`tadxodrzoquugnsyejld`) selesai 2026-07-22. Cloud lama sengaja DIBIARKAN HIDUP sebagai jaring rollback tapi **aplikasi TIDAK membacanya**. Akses DB: `ssh minipc-gudang` + `docker exec supabase-db psql`. **Perubahan skema = proposal dulu, eksekusi hanya setelah konfirmasi user.** JANGAN drop `wa_sync_status` (masih dipakai bot Telegram).
- **Stack docker-compose di minipc-gudang (6, jangan saling-tumpuk edit):** `vps-backup` (pg_dump `-Fc` public tiap jam + `backup-auth-storage.sh` harian 02:00 schema auth/storage/vault + `mirror-to-disk2.sh` ke `/mnt/backup2`); **`vps-dr-stack` = PRODUCTION AKTIF sesungguhnya** (koreksi 2026-07-28 — bukan "standby" seperti catatan lama; traffic production benar-benar lewat sini, jangan dimatikan); `vps-monitor` (healthcheck GoTrue, alert Telegram); `vps-observability` (Prometheus/Grafana/Alertmanager, LAN-only); `vps-staging` (Caddy static); `vps-remote-ssh` (tunnel Cloudflare SSH). SSH host: `minipc-gudang` (LAN 10.91.21.202, static sejak 2026-08-09; alias `~/.ssh/config` sempat basi di .231) / `minipc-gudang-home` (tunnel `ssh-admin.warnoto.com`). Disk root 879GB, `/mnt/backup2` 445GB. Supabase self-host TIDAK punya PITR — dump ini jaring utama.
- **JANGAN test via `curl` langsung ke `/rest/v1/*` di `warnoto.com` dengan ID mirip data asli** (pernah menimpa baris stok produksi, 2026-07-22). Pakai prefix `TEST-` konsisten atau container Postgres scratch terpisah.
- **Domain:** akses produksi = `pln.warnoto.com` (`warnoto.vercel.app` redirect 308). Apex `warnoto.com` = endpoint DB self-host (`VITE_SUPABASE_URL`, Cloudflare Tunnel) — **JANGAN disentuh**.
- **Realtime Data Stok self-host aktif** — publication `supabase_realtime` berisi hanya `public.stocks`.
- Trigger `on_auth_user_created` (auth.users→stub profiles) sudah di-re-create di self-host (fix akun "setengah jadi", 2026-07-30).
- **Triad katalog MENGIKAT (2026-08-14, keputusan user):** `no. katalog` — `nama material` — `satuan` adalah **1 kesatuan, tidak boleh terpisah**; sumber kebenaran = `mara_catalog` (master MARA SAP). Satuan (dan nama) katalog WAJIB ikut MARA berdasarkan `kode_material` (match dengan strip leading-zero: `ltrim(kode,'0')`). Saat isi/import/buat katalog, jangan biarkan satuan kosong atau beda dari MARA. **ENFORCEMENT = TRIGGER DB (2026-08-14, migration `20260814_katalog_satuan_from_mara_trigger.sql`).** GOTCHA yang bikin ini perlu: fill satuan via SQL langsung ke `katalog.data` SIA-SIA — `katalog` diklobber full-sync app (`saveToCloud`) dalam menit. Trigger `katalog_fill_satuan` (`BEFORE INSERT OR UPDATE`) isi `satuan` dari `mara_catalog` (`ltrim(kode,'0')`) saat kosong → menangkap tulisan app juga, non-destruktif. Uji clobber lolos (kosongkan → terisi ulang). Nama sudah dari MARA (match manual saat insert); trigger fokus satuan (kolom yang drift). Kalau mau nama ikut di-enforce trigger juga, itu keputusan terpisah.
- **Paket katalog 5-tuple terkunci saat `_maraLocked` (2026-08-14, keputusan user):** Triad di atas diperluas jadi `no.katalog — nama — kategori — satuan — status` — saat katalog bersumber MARA, kelimanya terkunci sebagai satu paket, tidak bisa diubah satu-satu di form (UI-only via `_maraLocked`, sudah di-strip saat save). SATU exception: Status boleh di-set manual jadi "Non-SAP" lewat checkbox — material non-SAP = kandidat yang akan dipindahkan ke SAP, boleh pakai katalog Persediaan/Cadang MARA yang sudah ada.
- **Material Cadang bisa UIT-scoped (2026-08-14, `8b37000`):** `material_cadang_state` +kolom `uit_id`; helper RLS `can_access_uit(uit_id)` (SUPERADMIN/PUSAT, uit cocok, atau UPT di bawah uit); policy `can_access_upt(upt_id) OR (uit_id AND can_access_uit(uit_id))`. Baris `uit_id` terisi = analisa level UIT, dibaca SEMUA UPT dalam UIT (spare transmisi dikelola UIT). Data existing SBY di-re-scope `uit_id=UIT-JBM`. Client 2-lapis: `mcMerge` (App.jsx) stamp `__uptId/__uitId` dari baris ke tiap record saat load; `inMcScope` (MaterialCadangTab) + cockpit ForecastStokPage kenal `__uitId===userUit` (`userUit`=`currentUser.uitId||uptList[user.uptId].uitId`); record baru default UIT-scoped. Per-UPT lama (`uit_id` null) tetap jalan.
- **Material Cadang durable per-UPT (2026-08-10):** tabel `material_cadang_state(upt_id PK, data/health/ai jsonb)`, RLS `can_access_upt(upt_id)` (pola `stocks`). Simpan 3 blob localStorage verbatim per-UPT. UI: server sumber utama (di-load lewat RLS scope, merge antar-row untuk nasional), localStorage fallback + seed sekali kalau server kosong (recovery data lama). Ganti jalur mati `syncMaterialCadangRows`→tabel per-row yang tak pernah ada (gagal senyap). AKAR bug "Material Cadang hilang": dulu localStorage-only → lenyap saat Clear site data / ganti device.

### Keamanan / role
- **ADMIN = vendor security pihak-3 (user 2026-08-12, mengikat):** ADMIN diturunkan jadi minim otoritas untuk input awal. Dalam UPT sekarang **TL > ADMIN**. ADMIN HANYA boleh: ajukan transaksi TUG, upload foto Data Stok, pindah blok/lokasi gudang, cetak Kartu Gantung. TIDAK boleh: edit/hapus Data Stok, ATTB, master data, kelola akun, approval, import. Semua kemampuan edit/kelola dipegang TL. Diberlakukan lewat (a) matrix `role_permissions` DB (ADMIN cuma `aksi.buatTransaksi`+menu dasar, `menu.attb` OFF; TL full) dan (b) gate hardcode di `App.jsx` (Edit/Hapus Data Stok + usul-blok OCR → `hasRole("TL")`), plus `DEFAULT_PERMS` (`perms.js`) diselaraskan sbg fallback bila matrix di-Reset. `canUploadFoto`/Pindah Blok sengaja tetap ADMIN+TL. **SEMUA gate management Master Data yang dulu `hasRole("ADMIN")` sudah diflip ke `hasRole("TL")`** (MasterDataTab.jsx: Kelola Denah & Koordinat gudang/sub-gudang, tambah blok, edit/hapus katalog/satpam/tim-mutu/UIT/UPT/ULTG/gudang/lokasi, sub-tab Migrasi Data & Audit Log; App.jsx: edit/hapus Lokasi kartu + Hapus item ATTB). ADMIN tak bisa akses krn `menu.master`/`menu.attb` OFF; ini supaya TL yang punya kuasanya (bug 2026-08-12: TL tak bisa upload denah/blok krn gate masih ADMIN).
- **HIRARKI PERAN RESMI (user 2026-08-02, mengikat, jangan ditafsir ulang):**
  - **UPT (lihat 1 UPT sendiri):** `ADMIN`, `TL`, `ASMAN`, `MANAGER`, `MGR_ULTG`, `ADMIN_ULTG`. Tiap UPT tepat SATU MANAGER. **MANAGER BUKAN Pusat.**
  - **UIT (lihat semua UPT):** `ADMIN_UIT`, `ASMAN_LOG_UIT`, `MGR_LOGISTIK_UIT`.
  - **Pusat (lihat semua UPT+UIT):** `ADMIN_LOG_PUSAT`.
- **GOTCHA role baru:** `can()` (`src/lib/perms.js`) return `false` utk role tak terdaftar di `DEFAULT_PERMS` → app kosong tanpa menu. Titik daftar role yang WAJIB sinkron: `roles.js`, `perms.js` (MATRIX_ROLES+DEFAULT_PERMS), `App.jsx` (UIT_ROLE_QUOTA + deteksi form scoped-UIT + `canSwitchMaturityUpt`), `AkunModals.jsx`, `scripts/bulk_create_users.mjs`, `schema.sql`, + 2 Edge Function (`admin-create-user`, `maturity-drive`) yang perlu **REDEPLOY terpisah**.
- **Helper scope 3-tier:** `getScopeUptIds(user,uptList)` → `null` (Pusat/SUPERADMIN=semua) | array upt id (UIT: semua UPT di `uitId`; UPT: `[uptId]`). Sumber tunggal `dataScope` di App.jsx, `scoped*` di-oper ke tab.
- **RLS `satpam` di-scope per-UPT (2026-08-19, mengikat):** dulu policy `satpam` = semua authenticated baca/tulis SEMUA UPT (satpam tak punya kolom upt_id, cuma `data->>'gudangId'`). Mismatch dgn `gudang` yang sudah di-scope `can_access_upt` → TL UPT-SBY ke-load satpam UPT lain & muncul palsu "Belum di-assign gudang". Fix: helper SECURITY DEFINER `satpam_gudang_upt(gudangId)` + policy `Scoped all satpam` = `can_access_upt(satpam_gudang_upt(data->>'gudangId'))` (USING+WITH CHECK). Migration `supabase/migrations/20260819_satpam_scope_per_upt.sql` — SUDAH di-apply ke DB self-host. Definisi: user lihat satpam iff boleh akses UPT gudang-nya; satpam tanpa gudangId hanya untuk Pusat/SUPERADMIN. Verified RLS: widi(TL-SBY)=19 satpam semua SBY, pusat/super=38, insert cross-UPT ditolak. **Pola sibling-master lain (tim_mutu dll) belum tentu ikut di-scope — cek per kasus.**
- Alur bisnis **review-first**; jangan auto-approve atau buat aksi turunan tanpa persetujuan.
- **Frontend dev role (Kevin `kevinnsetiawan`, user 2026-08-12):** boleh edit presentasi (`src/components/*`, `src/theme.js`, `src/index.css`, JSX `App.jsx`) + **push langsung ke `main` tanpa PR**. Zona terlarang: state/handler/`saveToCloud`/`src/lib/*`/`src/hooks/*`/schema/workflows/deps. Aturan lengkap di `.github/ROLE_FRONTEND_DEVELOPER.md`. **Proteksi `main` DILONGGARKAN**: required PR review + required status check `build` DIHAPUS (force_push tetap off). Jaring rollback: Vercel Instant Rollback (UI) + tag `snapshot-YYYY-MM-DD` harian (`.github/workflows/daily-snapshot.yml`) + dump DB per-jam `vps-backup`.

### Fitur canonical
- **TUG canonical hanya TUG-8/TUG-9** (tabel/RPC self-host, review-first Admin→TL→Asman, nomor server dari counter UPT). **TUG-15/Laporan di luar scope canonical.** TUG legacy hanya baseline, tanpa replay stok.
- **Template PDF TUG-3/4/5/10 seragam gaya AppSheet TUG-9** (`docBuilders.js`). JANGAN ambil perubahan `App.jsx` dari PR Kevin (menghapus guard Mode Demo / tombol "Isi Data Contoh" ke form resmi).
- **Maturity canonical self-host:** `maturity_assessments`, `maturity_audits`, `maturity_audit_history` (unik per upt/tahun/semester), `maturity_5s_assessments` (append-only, authenticated hanya SELECT+INSERT). Evidence audit = Google Drive binary + Supabase metadata; root folder `UPT Surabaya Apps` (`13FFto2pzVRLq4LBpRaJsIyGa2Bk5gaYD`), OAuth cred hanya di Edge Runtime MiniPC. Scope dari `maturity_audits.upt_id`; UIT hanya UPT se-`uit_id`; audit FINAL immutable. Mode Demo Maturity sengaja dimatikan.
- **Inspeksi Material Cadang:** self-host canonical + tenant boundary UPT/gudang ditegakkan di UI & DB, UPT/Manager dari profil login, runtime non-E2E menolak endpoint selain `warnoto.com`. Struktur: parent `material_inspection_batches` + `batch_id` pada `material_inspections`, nomor server `000001/BA-INSPEKSI/UPT-SBY/07/2026`, tepat 2 foto/material, satu BA satu gudang. Migration security `20260802_material_inspection_multi_upt_security.sql` **sudah applied**.
- **Perubahan lokasi Data Stok oleh ADMIN:** dalam Gudang sama → langsung simpan; lintas Gudang → butuh approval TL (lokasi lama utuh sampai disetujui). TL lintas Gudang → butuh ASMAN. Dropdown Gudang memfilter Blok/Lokasi tujuan agar `gudangId`/`lokasiId` tak silang.
- **Forecast Stok:** gabung histori legacy TUG-15 (`legacy_history_archive`) ke analisa AI, dicocokkan per katalog via `src/lib/normalizeKatalogCode.js`. Query legacy di try/catch TERPISAH (gagal ≠ gagal analisa). Cap histori -18 bulan. Metrik dihitung kode dulu lalu disisipkan ke prompt Groq; ada fallback lokal saat Groq gagal. Toast throttled saat auto-sync bot gagal.
- **Telegram** pakai Groq key hanya dari `.env` self-host (jangan taruh nilai di repo/HANDOFF). HTTP 400 parse-entity → retry 1x plain text.
- **Import "SAP Langsung" (Migrasi Data > Cara lain / Legacy)** — dari export SAP resmi (sheet "UPT LAIN SAP", header row 6). Ambil hanya Matl Type "Stock"→jenisBarang `Persediaan` / "Cadang"→`Cadang`. Plant→UPT via `SAP_PLANT_TO_UPT` (`src/data/masterUpt.js`: 3611=SBY,3612=MLG,3613=MDN,3614=PBG,3615=BLI). Scope pakai `uptList` yang sudah difilter parent (super admin=semua, user UPT=UPT sendiri; baris lain "diabaikan"). **ATURAN MENGIKAT re-import (anti-berantakan):** match stock by `(uptId, katalogId)` SAJA — JANGAN ikutkan lokasiId (dulu bug: id `STK-SAP-plant-material` tak ikut lokasi → collision saat blok sudah diisi). **`qty` = angka hidup** (dimutasi TUG/opname): material BARU insert `qty=SAP`; material SUDAH ADA `qty`/`lokasiId`/`id` TAK disentuh, cuma `sapBaselineQty`+`sapBaselineAt`+identitas-kalau-kosong. Preview per-baris + checklist (default centang hanya "Baru") + tombol Batal + kolom Selisih (SAP vs aktual). Katalog global `KAT-<material>` (buat bila belum ada). Riwayat import dibaca dari `audit_log` (`entity="migrasi_sap_langsung"`). Wizard cutover Surabaya lama (Plant 3611 hardcode) sudah dihapus. Fungsi cutover `approveMigrasiPending`/`migrasiPendingReview` SENGAJA disisakan (jaga item PENDING lama di DB, jangan hapus tanpa cek DB).

- **TUG-3/4 Barang Masuk → DB (2026-08-21):** dipindah dari blob localStorage `pln_txns_v3` ke tabel dedicated **`tug3_transactions`** (`{id,upt_id,created_by,doc_number,stage,status,data jsonb}`, RLS per-UPT `can_access_upt`, migration `20260821b_tug3_transactions.sql` **applied**). SENGAJA BUKAN canonical `tug_transactions` (itu OUT-only + hard-gate TUG8/9 + wajib stock_id lama). Sync via `src/lib/tug3Sync.js` (load/upsert/delete, fail-safe, tak sukses palsu); foto→Storage URL via `processTxnPhotos`. Blob legacy tetap dibaca read-only (dedupe by `id`, flag `canonical3`). **Alur baru:** DRAFT→PENDING_TL (TL) → MENUNGGU_TUG4 (isi TUG-4+lampiran satu modal) → PENDING_ASMAN (Asman, **stok MASUK di klien** `approveTUG3Final_Asman`) → APPROVED. **Approval Manager/MUP DIHAPUS** (TTD Manager di BAP tetap — itu tanda tangan, bukan gerbang). Edit-until-TL (pembuat, stage DRAFT|PENDING_TL, nomor dok dipertahankan). Draft langsung dapat nomor dok. Ajukan ke TL divalidasi lengkap KECUALI Amandemen (no+tgl) & foto lampiran.
- **Dokumen TUG-3/4 = format resmi PLN** (`buildTUG3HTML`, `docBuilders.js`) — supersede "seragam gaya TUG-9" di atas untuk TUG-3/4: Bon Penerimaan Barang + Berita Acara Pemeriksaan (pemeriksa dari Tim Mutu) + Lampiran Foto (kendaraan/SIM/surat jalan/foto barang). TTD Asman Konstruksi+TL (bon), Manager (BAP). Cabang **Karantina** (`!txn.approvedByAsman`) TTD "Diserahkan oleh"=supplier + "Diterima oleh"=TL; **Penerimaan** (final) TTD lengkap. BAP hanya muncul bila `txn.timMutuId` terisi.
- **Master Supplier** tabel **`supplier`** (`{id,data jsonb}`, RLS authenticated nasional, migration `20260821_supplier.sql` **applied**) — vendor bersama antar-UPT. Form TUG-3 "Dari (Supplier)" = `SearchableSelect` dari master + "+ Tambah supplier baru" inline. CRUD di Master Data.
- **TUG-10 (barang kembali/retur) → tabel `tug10_transactions`** (2026-08-24, migration `20260824` **applied**, RLS per-UPT `can_access_upt` + GRANT service_role, `src/lib/tug10Sync.js` — parity `tug3_transactions`). **TUG-8/9 (barang keluar) tetap CANONICAL `tug_transactions`** (server-atomik `createAndSubmitCanonicalTug`/`decideCanonicalTug`, decrement stok di server — JANGAN pindah/ubah). **Draft TUG-8/9 = blob-only** (`pln_txns_v3`, stage DRAFT `canonical:false`) sampai "Ajukan" → promote canonical (`submitDraftTug9`); **draft TUG-3/10 = tabel dedicated** stage DRAFT. **Bongkaran ATTB (MTU) di TUG-10 saat approve → auto buat record ATTB** modul (jenisAset MATERIAL, stage USULAN_AE1, `source:"TUG10"`+`sourceTxnId`/`sourceItemIdx` idempoten). **Foto JANGAN base64 di kolom jsonb `data`** — `upsertTug3/10Transaction` jalankan `processTxnPhotos` (upload Storage → URL) sebelum simpan (dulu 1 baris tug3 8.6MB base64 → `loadTug3` 22s; script `migrate_tug3_base64_to_storage.mjs` sudah bersihkan). Ukur reload lambat via **Resource Timing** (`performance.getEntriesByType('resource')`), bukan read_network (tak kasih durasi). **Semua dokumen TUG kunci `@page{size:A4}` + reset `@media print`** (cetak dari HP tak ikut lebar layar). **Auto-sync katalog/stok (`App.jsx` effect) pakai signature-guard** (`src/lib/syncGuard.js`) + incremental — JANGAN re-push full katalog/stock_current pasca-reload (dulu 503 write-storm). **`canonicalKatalogCode`** (`normalizeKatalogCode.js`) = normalisasi kode katalog utk DISPLAY + matching SAJA (DB tak diubah, QR aman). **`docSeqGuard.nextSafeDocSeq`** = nomor dok never-regress.

### Proses & tooling
- **Commit, push, deploy = tahap TERPISAH.** "kerjakan"/"lanjutkan"/"commit" BUKAN izin push. Push ke `main` (auto-Vercel) HANYA setelah user eksplisit bilang `push`/`deploy`. JANGAN `vercel --prod` (folder `outputs/` berat ikut).
- **Pembaruan `HANDOFF.md` wajib izin user dulu**; jangan otomatis.
- Tailwind v4 via PostCSS, preflight OFF; interaktivitas via CSS global element-selector.
- Sidebar desktop 260/76px, drawer mobile ≤768px.
- **`graphify`** = peta baca codebase (AST-only, lokal). Query dulu sebelum grep; `graphify update .` wajib setelah edit kode.
- **Classifier auto-mode memblokir commit yang menyentuh `.claude/settings.json`** — user jalankan sendiri (via `!` prefix). Bukan bug, pembatasan keamanan.
- **Dashboard tab routing per role:** "Ringkasan & Kinerja" → `ExecOverview` (semua role); "Overview Gudang" → `DashboardManager`/`DashboardAsman`/`DashboardDefault` per role. Ubah 1 komponen → invisible ke role lain; kalau diminta "perbaiki overview gudang" cakup ketiganya.
- **Area ATTB UI** (`AttbTab.jsx`, `AttbDashboardSummary.jsx`, styling terkait) pernah diserahkan ke tim user — koordinasi dulu sebelum menyentuh.
- **App.jsx besar** (~6400 baris); ekstraksi internal PLNWarehouse ke custom hooks = keputusan berisiko, hanya jalan setelah persetujuan user + verifikasi browser.
- Vendor C = OpenCode Go (backup ke-3 setelah Claude→Codex→GLM, manual).

## Status sekarang

- **Live Location Alat Berat (crane/truck/manlift) via GPS HP operator — SELESAI + PUSHED (`4512e33`→`e0b9a02`, 2026-09-03), semua migration APPLIED, integrasi terverifikasi API-layer. Sisa: smoke-test browser device nyata.** Plan `C:\Users\PLN\.claude\plans\lanjut-2-perbaikan-cheerful-stonebraker.md`.
  - **Migrations APPLIED self-host:** `20260903` (equipment_location realtime + equipment_trip + operator_profile), `20260903b` (operator_profile.profile_photo), `20260903c` (equipment_trip.inspection). RLS masih permisif-authenticated (TODO ketatkan per-UPT). EF `admin-create-user`/`admin-update-user` +OPERATOR di VALID_ROLES (deployed).
  - **Alur operator final:** login OPERATOR (bootstrap RINGAN — cuma upt+heavy_equipment) → 3 menu (Sesi Kerja / Profil / Riwayat). Sesi Kerja: pilih unit (kartu, lain auto-hide + "Ganti unit") → checklist inspeksi 12 item (`INSPEKSI_ALAT`, wajib semua centang) → konfirmasi Berangkat → peta live titik + Wake Lock (layar nyala) → konfirmasi Stop → ringkasan sesi + "Mulai Pekerjaan Lagi". Trip+inspeksi tersimpan `equipment_trip`. Profil: nama+HP+foto avatar/SIO/SIA. Riwayat: trip operator sendiri + rute polyline.
  - **Peta dashboard internal:** toggle "Alat Live" marker per-unit realtime (MOVING/STOPPED/stale). Panel Riwayat Perjalanan (admin/TL) semua trip UPT.
  - **DITUNDA:** background GPS sungguhan (app ditutup) = fase native Capacitor (web = wake-lock best-effort, jeda saat pindah app). Item inspeksi = draft (user boleh revisi). RLS per-UPT hardening.
  - **SISA:** smoke-test di device HP nyata (GPS gerak, wake-lock, peta) — di localhost pakai DevTools→Sensors→Location.
  - **Role OPERATOR** (`roles.js`/`perms.js`): 2 menu (Lacak Alat + Profil), scope UPT, nol aksi tulis. Nav `App.jsx` cabang operator. Akun dibuat via Kelola Akun (dropdown role generik).
  - **Alat**: field `kategori` (eksplisit, incl Truck) + `tracked` (jsonb); `getEquipmentCategory` prefer `e.kategori` else tebak-nama.
  - **Operator app** `EquipmentLiveShare.jsx`: pilih unit tracked UPT-nya → Mulai → `watchPosition` hemat baterai (highAccuracy off, throttle 25s + distance-filter 25m) upsert `equipment_location` MOVING → Stop = kunci STOPPED + insert `equipment_trip` + ringkasan (durasi/km/titik). Auto-Stop ganti unit, cleanup clearWatch. `OperatorProfile.jsx`: nama+HP+foto SIO/SIA (apple-like, tabel `operator_profile`).
  - **Peta dashboard**: layer live per-unit (`_liveAlatLayer`, marker kategori 🏗️/🚚/🛗, MOVING/STOPPED, stale>5mnt redup), realtime `warnoto-eqloc-realtime` + `setLatLng` anti-flicker, toggle "Alat Live" (`DashboardRingkasanBlock`). Agregat lama utuh. **Riwayat Perjalanan** `RiwayatPerjalananPanel.jsx` (list trip + rute polyline). `geo.js` haversine + self-check. CSP `vercel.json` `geolocation=(self)`.
  - **WAJIB user:** (1) **apply migration** `supabase/migrations/20260903_equipment_tracking.sql` (3 tabel, self-host). (2) push ke main (CSP geolocation baru live via Vercel). (3) smoke-test: buat akun OPERATOR → login (2 menu) → set unit Kategori+Lacak → Mulai/Stop (DevTools Sensors geolocation) → dashboard toggle Alat Live marker gerak realtime → Riwayat rute.
  - **DEFERRED (ponytail):** RLS `equipment_location`/`equipment_trip`/`operator_profile` masih **permisif-authenticated** (any authenticated read/write) — ketatkan per-UPT (operator hanya unit UPT-nya) di iterasi lanjut. Aman sementara (app internal), tapi catat.

- **Kapasitas Gudang Fase B (push App→Google Sheet operasional) — SELESAI + PUSHED `b6ed6fd` + EF DEPLOYED + TERVERIFIKASI API-layer (2026-09-03).** Sisa: smoke-test lewat app (login ADMIN/TL → tombol Push).
  - EF `supabase/functions/push-kapasitas` (`index.ts`+`mapping.mjs`): gate ADMIN/TL/SUPERADMIN, reuse OAuth `GOOGLE_DRIVE_*` (sama maturity-drive), resolve tab dari gid, baca grid A:C, `classifyRows` update(match H:P+S saja, tak timpa alamat/CP/dll) vs insert(area baru → append A:X full), angka RAW, pct fraksi. Mode `dryRun` untuk preview. Sheet `1GND76s...` gid 361941646 = tab "KAPASITAS GUDANG". **Arah RESMI 1-arah App→Sheet** (jangan jalankan `sync-kapasitas` Sheet→App atas kolom sama setelah push).
  - Client `KapasitasGudangTab.jsx`: tombol "⬆️ Push ke Google Sheet" (gate canEdit) → dryRun preview → confirm "N update, M baris BARU" → push → toast / 403 actionable. `toPushRow` camelCase→snake_case. Self-check `scripts/check-push-kapasitas-map.mjs`.
  - **DEPLOY EF**: copy `index.ts`+`mapping.mjs` ke volume `functions/push-kapasitas` via `ssh minipc-gudang-home` (md5 identik, edge-runtime load per-request).
  - **INFRA (SUDAH BERES 2026-09-03):** (1) Google Sheets API **di-enable** user di project `1005789872180`. (2) Scope OAuth **sudah cukup** (`https://www.googleapis.com/auth/drive` full — TAK perlu re-consent). (3) Akun OAuth `logistik.tjbtb@gmail.com` = **OWNER** sheet (`canEdit:true`, TAK perlu share Editor). Terverifikasi via box: token mint OK, `spreadsheets.get`/`values.get` 200, write `values.update`+`clear` 200 round-trip (cell scratch AK1039, dibersihkan). Semua operasi EF terbukti jalan.
  - **CEILING (ponytail):** salah-insert bikin baris dobel kalau key app (UPT|GUDANG|SUB, `upt`=kota) ≠ ejaan sheet — dijaga preview dryRun+confirm. Push kirim SEMUA row `gudangCapacityList` (scope viewer).

- **Stock Opname Fase 3 (freeze/unfreeze mode PERINGATAN) — KODE SELESAI, build hijau + self-check 5/5, BELUM commit + belum verify browser (2026-09-02).** Plan walrus Fase 3.
  - `useStockOpname.js`: field sesi `freeze:{aktif,gudangIds[],at,by,unfrozenAt}` (sesi lama null aman via `?.`); handler `setOpnameFreeze(opn,{aktif,gudangIds})` guard ADMIN/TL/ASMAN, persist pola `saveToCloud`; `approveOpname_Manager` auto-unfreeze saat SELESAI.
  - `useTugTransactions.js`: **cek freeze dipindah dari `saveTxn`→`commitNewTxn`** (titik konvergensi terluas: semua callsite saveTxn + submitDraftTug3/10 lewat sini). `collectTxnGudangIds` (TUG8/9 `formData.gudangId`; TUG3/10 turunkan dari `lokasiTujuanId`+`stockItems[].lokasiTujuanId` via `lokasiList`) + `findActiveFreezeSession`. Ketemu → `window.confirm`; Batal=return (tak simpan); Lanjut=stamp `freezeWarnOpnameId` di jsonb. TUG5 diabaikan (bukan gudang-spesifik). Field names terverifikasi.
  - `App.jsx`: oper `lokasiList,gudangList,opnameList` ke `useTugTransactions`; `setOpnameFreeze` ke StockOpnameTab. `StockOpnameTab.jsx`: badge 🧊 FREEZE + panel checkbox gudang (blue-card, gate role). Self-check `scripts/check-opname-freeze-match.mjs`.
  - **SISA:** verify browser (login ADMIN/TL/ASMAN → freeze gudang → TUG3/8/9/10 gudang itu munculkan confirm; Batal tak simpan; Lanjut simpan+`freezeWarnOpnameId`; approve Manager → unfreeze). SUDAH commit+push `0b4830a`.

- **▶ BESOK DI KANTOR (verify browser, 3 fitur baru + Stock Count E2E):** (a) **Stock Count 1 siklus penuh** — user mau coba upload SAP → bandingkan → temuan → approval Asman → beres; DITUNDA karena file SAP belum di-download (besok di kantor). localhost:3001 = DB self-host PRODUCTION (data nyata, akun Raudy/UIT login). (b) verify Fase 3 freeze opname (langkah di atas). (c) verify PDF Form 5S siap-TTD (`2b107b4`) + grafik history audit target-line nyambung. Semua kode sudah push ke main (auto-deploy Vercel), tinggal uji runtime.

- **2 perbaikan Maturity — COMMIT `2b107b4` (belum push) + belum verify browser (2026-09-02).** Panel notif Master Data `aa2c61d`. Plan `C:\Users\PLN\.claude\plans\lanjut-2-perbaikan-cheerful-stonebraker.md`.
  1. **PDF Form 5S siap-TTD Asman:** builder baru `buildForm5SHTML(record,users,uptList)` di `src/lib/docBuilders.js` (tiru `buildTUG9HTML` — A4 portrait, kop PLN, `esc()`, `.items-tbl`, `.sig-row-2`). Tabel checklist per-kategori + nilai `checked/total (%)` + baris TOTAL (="bobot" = proporsi centang, TAK ada bobot per-item), catatan, grid foto (`preview||url`), TTD Asman (`users.find(role===ASMAN && uptId cocok)`, else titik-titik). `handlePrint` (`MaturityAuditSystem.jsx:1667`) diganti: `window.open` sinkron → rakit record → `buildForm5SHTML` → write. Prop `uptId/users/uptList` di-drill via `MaturityDashboardTab`→`Form5STab` + `App.jsx`. **Simpan DB TAK disentuh** (sudah jalan: `maturity_5s_assessments`, semua UPT, isolasi upt_id). **Batas diketahui (ponytail):** foto hanya render saat fresh-submit (blob preview); cetak-ulang History butuh EF blob-proxy `driveFileId` (ditunda). CSP: string `onclick="window.print()"` dipertahankan persis (hash-locked).
  2. **Grafik History Audit distyle** (`MaturityDashboardTab.jsx:359-424`, presentasi saja, NOL ubah data/target/input): bar warna dinamis (hijau skor≥target / kuning-merah di bawah), garis target dashed dipertegas, gridline skala 0-5, legenda mini Target/Pencapaian, palet dari `theme.js`.
  - **SISA:** verify browser (login Admin/TL UPT → Form 5S isi+simpan+Cetak PDF: layout A4, foto, TTD; grafik dashboard tercapai/belum + set target). Lalu commit+push.

- **▶ LANJUT MALAM 2026-09-02 (catatan UIT-JBM — 6/7 item live `d9e0105`, lihat log paling bawah):**
  1. **Item A/B E2E** (code sudah live, belum diuji runtime): buat 1 TUG-3 + 1 reservasi ULTG **dummy** (tandai buang), approve TL→Asman → cek (A) `kontrakRefs` muncul di detail Data Stok material itu, (B) badge "✅ Siap diambil" nyala di kartu reservasi ULTG yang katalognya cocok. Butuh login peran TL/Asman UPT (bukan UIT).
  2. **Item 2 (notif WA/Telegram) — SELESAI + LIVE (2026-09-03).** Provider **Fonnte** (gateway tak-resmi). `sendWhatsApp()` isi Fonnte (`POST api.fonnte.com/send`, header `Authorization` token), `buildMessage` async (daftar material `tug_items`→`stocks`→`katalog` + tanggal, fallback ringkas). EF `notify-dispatch` **deployed** self-host + env `FONNTE_TOKEN` di `.env`+compose passthrough (`docker-compose.yml.bak_fonnte`), container recreated. **Pemanggil = client-invoke** (App.jsx `approveTxn` canonical, setelah TUG-8/9 FINAL_APPROVED, fire-and-forget) — BUKAN pg_cron (belum ter-install, butuh preload+restart). Tombol "Kirim notif tertunda" (drain manual) di `NotifRecipientPanel`. **Tes WA end-to-end LULUS** (insert outbox uji→invoke→Fonnte→WA sampai 628…). **SISA (user):** isi nomor akuntansi asli di panel (Master Data→Kelola Akun→Kelola Penerima Notifikasi, channel WA `628xx`); approve TUG-9 asli → WA + daftar material. Telegram channel juga siap (recipient channel TG).
  Detail lengkap: plan `C:\Users\PLN\.claude\plans\ada-catatan-tambahan-dari-lazy-mochi.md`.

- **Akun PLN Pusat (ADMIN_LOG_PUSAT) + Maturity nilai final per-item — PUSHED (2026-08-28, `a539d76`+`870c9a7`), migration APPLIED. Belum verify browser.**
  - **Part 1 akses (`a539d76`):** PLN Pusat kini baca nasional semua UPT/UIT di Kapasitas Gudang + dropdown gudang Data Stok. `inspectionScope.mjs` helper `isNationalRole` (SUPERADMIN+ADMIN_LOG_PUSAT) di `getVisibleGudangForInspection`/`getInspectionScope`; `App.jsx:3621` `gudangAccessLimited=dataScope!==null`. Jalur `dataScope` stok sudah nasional sebelumnya (roleTier PUSAT). **Perms TAK diubah** — PLN Pusat = `{...FULL_MENUS}` NOL `aksi.*`, read-only (tak ada kelola akun/master). Tombol close ✕ Detail Kapasitas → persegi 36×36.
  - **Part 2 Maturity (`870c9a7`):** Pusat beri nilai final 1-5 **per ITEM evidence**. Migration `20260828_aspect_review_final_score.sql` (kolom `final_score` di `maturity_aspect_reviews`) **APPLIED self-host** (via `ssh minipc-gudang-home` tunnel — LAN `minipc-gudang` sempat timeout; RLS Pusat sudah boleh update, tak berubah). Widget `PusatScoreControls` (segmented 1-5) per item saat `canScorePusat&&isUploaded&&!isAutoFilled`; `setAspectItemScore` merge review UIT + agregasi mean→`aspekScores.pusat` (exclude auto) → `calcMaturityScore` existing. Alur: tombol **UPT** "Ajukan Penilaian Final ke Pusat" (UIT "Kirim ke Pusat" DIHAPUS); Finalisasi Pusat digate `allItemsScored`. RLS `maturity_audits` sudah izinkan UPT transisi SELF_ASSESSMENT→REVIEW_PUSAT (verified USING status-current + WITH CHECK longgar).
  - **SISA:** verify browser (login Kresna: Kapasitas semua UPT, close simetris; alur maturity UPT→Pusat scoring→Finalisasi). Ceiling minor: `aspekScores.pusat` turunan tak di-recompute dari reviews saat reload sebelum finalisasi (Pusat skор+finalisasi 1 sesi = aman).

- **Kapasitas Gudang Fase A — PUSHED (2026-08-28, `e983d08`). Belum verify browser.** Menu `KapasitasGudangTab.jsx`. (1) Foto gudang: field baru `fotoGudang` (base64, `compressImage` 600px/200KB) diupload di Master Gudang add+edit (pola foto Satpam), tersimpan di `data` jsonb gudang (TANPA migration — gudang = jsonb blob); Detail kapasitas tampil foto via `matchedGudangId` (fallback `fotoGudang`→`denahImageData`→placeholder). (2) TL edit: tombol Edit (gate ADMIN/TL/SUPERADMIN) di tabel Data + detail → modal, hitung ulang `sisaLuasM2`/`persentaseTerpakai`/`statusKapasitas` (threshold 0.75/0.90) preview live, simpan `saveCapacityRow` (App.jsx:1827)→`syncWarehouseCapacity`+audit. (3) Responsif sudah OK (index.css `.capacity-*` collapse 768/card-mode) — tak ditulis ulang.
  - **DEFERRED — RLS `warehouse_capacity`**: masih terbuka (`authenticated for all`, schema.sql:512). Hardening ke ADMIN/TL/SUPERADMIN DITUNDA karena akan memutus `approveCapacityImport` (Asman/Manager/ADMIN_UIT/dll menulis kapasitas saat "Setujui & Publish" via sesi user, ApprovalTab). Fix benar = pindahkan approve-publish ke jalur service_role/EF DULU, baru gate RLS. Client-gate `canEdit` sudah kunci UI edit.
  - **PENDING — Fase B (sinkron App→Google Sheet)**: nunggu infra user: (1) fileId sheet tujuan (kandidat sumber `1GND76s06KHIWtwLrmnmNPBmn46sDRJD9kVre5F5_sq8` gid 361941646, dipakai EF `sync-kapasitas` arah Sheet→App), (2) share Editor ke akun OAuth `logistik.tjbtb@gmail.com`, (3) scope `spreadsheets` (token sekarang kemungkinan `drive.file` — perlu re-consent). Rencana: EF `push-kapasitas` pakai Sheets API `values.batchUpdate` (cell-targeted, jangan Drive files.update yang menimpa multi-tab). Detail di plan `C:\Users\PLN\.claude\plans\tl-bisa-edit-file-eventual-flute.md`.

- **Form TUG-8/9 (5 perbaikan) + redesign evidence Maturity + rename ATTB→MRWI — PUSHED (2026-08-28, `fc8e847` + `0ce5db4`). Belum verify browser (renderer beku saat screenshot halaman berat; MRWI sidebar sempat live-verified).**
  - **`0ce5db4` TUG-8/9** (`TugFormModals.jsx`, `useTugTransactions.js`, `sap.js`, `SearchableSelect.jsx`, `utils.js`, `TransactionHubTab.jsx`, `App.jsx`): (1) search material pintar — `matchesMaterialSearch` +substring tengah kata +kode leading-zero-insensitive; `SearchableSelect` +ranking relevansi (stable sort) +cap 50→100 (FUNGSI BERSAMA, aditif). (2) qty buffer string anti-0-depan (TUG-8/9 + TUG-10). (3) foto material inline per kartu barang (seksi bawah dihapus, `fotoMaterial` keyed stockId tetap). (4) scope gudang tiru TUG-10 — selector "Gudang Sumber Barang" filter material (`enrichStock` +`gudangId`) & satpam (`sp.gudangId`), auto-pick satpam tunggal, ganti gudang reset barang; `editCanonicalTug98` backfill `gudangId` dok lama. (5) tombol "Perbaiki" TL juga di History (`TransactionHubTab`) untuk PENDING TUG-8/9/10 (`editCanonicalTug98` diprop-drill). **Edge minor:** dok TUG-8/9 lama lintas-gudang saat Perbaiki → barang gudang lain bisa tak muncul di picker (nama tetap di ringkasan). **Gotcha:** perubahan search di fungsi bersama — verifikasi DataStok search tak regresi.
  - **`fc8e847` evidence+MRWI**: evidence Pelaksanaan Audit jadi list vertikal (row card apple-like, `MaturityAuditSystem.jsx`); menu ATTB→MRWI (label sidebar + Matrix Izin + hero "MRWI — Penghapusan Aset"; `App.jsx`/`perms.js`/3 e2e); id/key/tab/tabel/JENIS_BARANG "ATTB" & `AttbTab.jsx` TAK diubah. Isolasi upt_id evidence sudah aman 3 lapis (tak diubah).
- **TL bisa perbaiki (edit in-place) file TUG ajuan admin selama PENDING — KODE SELESAI + MIGRATION APPLIED self-host, build hijau, BELUM commit + belum verify browser (2026-08-28).** Plan `C:\Users\PLN\.claude\plans\tl-bisa-edit-file-eventual-flute.md`. Tujuan: admin sering salah input → TL betulkan langsung tanpa reject-recreate.
  - **Cakupan (Opsi 2, keputusan user):** tombol "✏️ Perbaiki" di kartu Approval, gate `hasRole(TL,SUPERADMIN) && status==='PENDING'`, hanya tipe di antrean TL (`requiredApprover=TL`) = **TUG-3/8/9/10 dari admin**. TUG-5 (antrean Asman) TIDAK dijangkau — layar TL tak diperlebar. Routing `editTug5`/`isEditInPlace5u/5` yang terlanjur dibangun = dead-but-harmless (siap kalau kelak mau Opsi 1).
  - **Canonical TUG-8/9:** RPC baru `tug_amend` (`supabase/migrations/20260827_tug_amend.sql`, migration-only) — guard `status=PENDING`+version+`role in (TL,SUPERADMIN)`+scope UPT, hapus+insert ulang `tug_items`, recompute hash (doc_number TETAP), event audit `AMENDED`, version+1. Ubah hash/version otomatis membatalkan review-token PREPARED (decide → `TUG_REVIEW_REQUIRED`). Client `amendCanonicalTug` (`tugCanonical.js`), routing di `saveTxn` (`useTugTransactions.js`) — kalau edit txn `.canonical` → `tug_amend`, bukan buat-baru.
  - **Legacy TUG-3/5/10:** edit in-place lewat blob (`commitNewTxn` `replaceDraftId`) — pertahankan docSeq/docNumbers/stage/approval fields, tak makan nomor. Fungsi `editDraftTug3`/`editTug10`(=editDraftTug10)/`editTug5`/`editCanonicalTug98` diprop-drill via `App.jsx`→`ApprovalHubTab.jsx`→`ApprovalTab.jsx`.
  - **APPLIED 2026-08-28:** nama constraint terverifikasi cocok default; migration di-apply via psql-pipe single-transaction (4× ALTER + CREATE FUNCTION + GRANT). Verified: `tug_amend(uuid,integer,jsonb,jsonb,uuid)` ada + `authenticated` execute; CHECK `event_type` punya `AMENDED`, `operation` punya `AMEND`.
  - **COMMIT+PUSH SELESAI (2026-08-28):** `f10c52f` fix popup window.open, `d3ba931` feat TL-edit-TUG, `baf644b` fix gate TUG-3.
  - **VERIFIKASI BROWSER (2026-08-28, login TL Widi asli, data produksi — TANPA simpan):** TUG-3 PENDING_TL → tombol "Perbaiki" muncul, form kebuka pre-filled (data asli). **TEMUAN+FIX (`baf644b`):** kartu TUG-3 stage MENUNGGU_TUG4 sempat ikut tampil Perbaiki → kalau diedit+simpan bakal renumber+regress (isEditInPlace cuma cek PENDING_TL); gate dibatasi `t.stage==='PENDING_TL'` utk TUG-3, terverifikasi live tombol hilang di MENUNGGU_TUG4.
  - **SISA:** TUG-8/9/10 belum ada sampel PENDING di antrean saat verifikasi → belum dites live (logika amend + build + skema migration sudah lolos). Kalau mau tuntas: buat 1 ajuan tiap tipe dari admin lalu TL Perbaiki, atau smoke SQL `tug_amend` (non-TL → `TUG_AMEND_FORBIDDEN`, token basi → `TUG_REVIEW_REQUIRED`).
- **Kartu Gantung print/save FIX + Review UX iterasi-2 — PUSHED (2026-08-26, `8af9235`).**
  - **Kartu Gantung (`KartuGantungModal.jsx`):** tombol Print/Save PDF tak jalan — `buildTUG2FrontHTML/BackHTML` async (await `QRCode.toDataURL`), `window.open` dipanggil SETELAH await = keluar user-gesture, popup diblokir (w=null). Fix: `window.open` dipindah ke AWAL handler (sinkron), isi HTML setelah await. Pola untuk dok lain yang builder-nya async juga.
  - **Review UX (`MaturityAuditSystem.jsx`):** (1) `AspectReviewControls` state-aware — setelah CHECKED hanya "Ubah ke Reject", setelah REJECTED hanya "Ubah ke Check" (badge status sudah wakili state). (2) Sisi UPT: badge terlihat (🔒 saat Checked); saat `itemReviewState==='CHECKED'` tombol upload/ganti + hapus file DIKUNCI (ganti badge "🔒 Terkunci — sudah di-Check UIT"). REJECTED/PENDING tetap boleh re-upload.
- **Review PARALEL per-item — MATLEV Audit System — PUSHED (2026-08-26, `49c5db3`). Migration di-apply self-host, terverifikasi browser.** File: `maturitySync.js`, `useMaturity.jsx`, `MaturityAuditSystem.jsx`, `MaturityDashboardTab.jsx`, `App.jsx`.
  - **Tujuan:** UIT (unit induk) bisa Check/Reject tiap **item bukti** (requiredEvidence) begitu diupload UPT, PARALEL dgn UPT yang masih mengunggah item lain — buang gate all-or-nothing lama. Submit akhir & check final tetap Pusat. Terisolasi per-UPT (RLS).
  - **Skema (2 migration, SUDAH di-apply prod):** tabel `maturity_aspect_reviews` PK `(audit_id, aspect_id, item_id)`, kolom `upt_id, state('PENDING'|'CHECKED'|'REJECTED'), note, reviewed_by, reviewed_at`. RLS: SELECT `can_read_maturity_upt(upt_id)` (UPT lihat sendiri, UIT/Pusat lintas); INSERT/UPDATE `can_review_maturity_uit() or _pusat()` **TANPA syarat status audit** (inti paralelisme); tak ada DELETE. **GOTCHA (bikin 403 "permission denied for table"): tabel baru WAJIB `grant ... to authenticated` DAN `to service_role`** — sempat lupa authenticated, di-fix via `20260826_...` + grant manual. Migration `20260826b` tambah `item_id` ke PK (tabel kosong = ALTER aman). Pelonggaran policy `"Maturity audits update by stage"`: cabang UIT diperluas ke status `SELF_ASSESSMENT/REVISION/REVIEW_UIT` supaya UIT bisa flip → REVIEW_PUSAT dari fase paralel.
  - **Alur baru:** UPT upload evidence (audit tetap SELF_ASSESSMENT, tombol "Kirim ke UIT" DIHAPUS) → UIT buka via dashboard (tombol "Review" kini muncul selama DRAFT/SELF_ASSESSMENT/REVISION, bukan hanya REVIEW_UIT) → Check/Reject per-item → UPT re-upload item Rejected (badge "perlu re-review" via staleness `evidence.savedAt > reviewedAt`) → semua item CHECKED (auto-filled Form 5S di-skip) → UIT "Kirim ke Pusat" (gate `allItemsChecked`+evidenceComplete+form5S) → Pusat Finalisasi → FINAL (trigger history existing).
  - **Client:** `loadAspectReviews`/`upsertAspectReview` (`maturitySync.js`, onConflict `audit_id,aspect_id,item_id`, +fix `AUDIT_STATUS` sempat tak ada REVIEW_PUSAT); state `maturityAspectReviews` keyed `` `${aspectId}::${itemId}` `` + `setAspectReview(aspectId,itemId,state,note)` (`useMaturity.jsx`); UI badge+kontrol per-item card evidence (dirombak compact/clean + responsif HP anti-overflow: stack kolom di HP, tombol full-width 40px, teks wordBreak) di `MaturityAuditSystem.jsx`.
- **Cetak BA Inspeksi Material Cadang — LAYOUT DIROMBAK + PUSHED (2026-08-26, `4205de1`).** Build hijau, terverifikasi via headless-Chrome print-to-PDF (2 halaman). File `src/components/InspeksiMaterialCadangTab.jsx`, blok `printBatch`.
  - **Akar "berantakan":** `.inspection-ba` pakai `position:absolute` → Chrome ABAIKAN `page-break-before`, semua konten numpuk 1 halaman (foto baris-2 kepotong). FIX: render BA via `createPortal(..., document.body)` + print CSS `#root{display:none}` / `.inspection-ba{display:block}` (blok normal-flow, paginate normal). `<style>` tetap apply walau di dalam #root yang display:none.
  - **Orientasi & foto:** `@page{size:A4 landscape;margin:12mm}`. Foto pindah halaman 2 (`section` dgn `page-break-before:always`), grid 3 kolom, tiap `figure` `break-inside:avoid`, tinggi 200px objectFit cover. **Preload semua foto (`new Image()` await onload) SEBELUM `window.print()`** — tanpa ini print fire sebelum signed-URL `<img>` load = foto kosong.
  - **Halaman 1:** judul + intro + tabel meta (Tanggal/Nama Gudang/No SLoc/Nama UPT) + tabel material + blok TTD. Tanggal pakai `printBatch.tanggal` mentah (tak reformat, sesuai data eksisting). TTD tiap pelaksana pemeliharaan TERPISAH (map array, bukan join 1 sel), jarak label→nama spacer 78px.
  - **No SLoc (melekat ke UPT, bukan gudang):** `slocForUpt(namaUpt)` map kota → Surabaya 2000 / Malang 2010 / Madiun 2020 / Probolinggo 2030 / Bali 2040 / Gresik 2050 (match `.includes(city)`, tahan variasi nama). Berlaku BA lama+baru tanpa ubah data/schema. Field `noSloc` di header BA tetap tak diisi form (map cukup).
  - Header/footer print (tanggal, url, `1/1`) = setelan dialog print browser (uncheck "Headers and footers"), bukan kode.
- **FORM 5S — catatan+foto WAJIB + wiring upload Drive + responsif tablet + fix nama item K3 — DEPLOYED + PUSHED (2026-08-25).** Build hijau.
  - **Guard isi:** checklist opsional (sesuai lapangan); **Catatan/Temuan/Tindak Lanjut** & **min 1 Foto Sampling** WAJIB sebelum **Simpan DAN Cetak/PDF** (`handlePersist`+`handlePrint`, `MaturityAuditSystem.jsx`).
  - **Upload foto (kontrak EF baru, mengikat):** action `upload-5s` di EF `maturity-drive` — non-audit (Form5S tak punya auditId), resolve UPT `findUptByName`+`assertUptAccess(true)` (scope UPT dijaga), folder chain `maturity-v1:{period}:{upt}:{period|upt|form5s}` via `ensureFolder`, upload `uploadDriveFile`, return metadata evidence saja — **TIDAK** tulis `maturity_audit_evidence`/`event()`. Root-init diekstrak `ensureRoot()` (dipakai `ensureTree`+`upload-5s`). Client `uploadForm5SPhoto()` di `maturityDrive.js`. Foto disimpan di field `samplePhotos` record assessment (tanpa tabel baru). **EF sudah di-deploy self-host** (md5 identik lokal↔remote, backup `.bak_form5s_*`).
  - **Responsif tablet (presentation-only):** akar = breakpoint tunggal `isMobile ≤768`, tak ada tier tablet → grid desktop di-crush di 820/834/1024px. Fix di `MaturityAuditSystem.jsx`: hook lokal `useIsTablet()` (≤1024, TANPA prop-drill/App.jsx); grid simetris → intrinsic `repeat(auto-fit,minmax(...))`; grid asimetris + tab → stack via `(isMobile||isTablet)`; tabel Checklist 5S pakai `compact=isMobile||isTablet` (tablet = layout mobile, hilang scroll horizontal).
  - **Fix nama item audit (`src/data/auditAspects.js`, hanya string `title` — subtext/evidence sudah benar):** K3 → 4.1↔4.3 title ketuker (fire-protection↔CCTV) ditukar, 4.4 "APD"→`"Ketersediaan Izin Kerja (Working Permit) untuk Pekerjaan Berisiko"` (isinya Working Permit, APD sebenarnya di 4.2). Sarana Prasarana → 3.1↔3.2 title ketuker (3.1 isinya area/prasarana gudang, 3.2 isinya alat angkut forklift/crane) ditukar, 3.5 `"Penyampaian Kondisi Sarana Prasarana..."`→`"Penyampaian Kondisi Gudang (Material Ex-Operasi)"` (isinya TUG10/ex-ops). Audit 30 item tuntas: mismatch hanya 4.1/4.3/4.4 + 3.1/3.2/3.5, sisanya cocok.
  - **BUGFIX upload-5s gagal (2026-08-25, root cause via repro browser):** error asli = `folder_type check constraint` tolak "FORM5S" (bukan timeout). Migration `supabase/migrations/20260825_drive_folder_type_form5s.sql` tambah 'FORM5S' ke CHECK `maturity_audit_drive_folders_folder_type_check` — **SUDAH di-apply ke prod self-host** (user jalankan via `!`; classifier blokir Claude tulis DB prod). "FORM5S" sengaja di luar filter scan sync (`ITEM`=evidence L393, `ROOT/PERIOD/UPT/CATEGORY/ASPECT`=unassigned L408) → foto Form5S tak mencemari evidence audit. **Verified 200 + preview OK di browser.**
  - **Tombol Kamera (2026-08-25):** slot upload foto Form5S kini 2 tombol — 📷 Kamera (`capture="environment"`) + 🖼️ Galeri (`multiple`). Pola PhotoSearchModal.
  - **Preview foto slot (2026-08-25):** `<img src>` pakai `URL.createObjectURL(File)` (`photo.preview`), bukan `webViewLink` (halaman Drive privat, bukan bytes). Cakupan: foto baru diupload sesi ini. **LIMITASI:** foto yang di-reload (History `<a>` link, atau draft nanti) belum bisa preview — foto Drive privat butuh proxy blob EF `download` diperluas terima `driveFileId` (+scope). Belum digarap.
  - **PENDING #3 — Simpan Draft Form5S (user pilih opsi B = DB):** desain: tabel BARU `maturity_5s_drafts` (upt_id, gudang, bulan, tahun, auditor, checklist jsonb, catatan, sample_photos jsonb, updated_at/by, UNIQUE(upt_id,gudang,bulan,tahun)) + RLS scope-UPT (mirip maturity, GRANT service_role juga [[selfhost-new-table-service-role-grant]]) — tabel final `maturity_5s_assessments` append-only DIBIARKAN. Client `saveMaturity5SDraft`/`loadMaturity5SDraft` (upsert + prefill saat buka form), tombol "Simpan Draft" di samping Reset/Cetak/Simpan; saat Simpan final → hapus draft. **Coupling:** preview foto draft yang di-reload butuh EF blob-proxy `driveFileId` (lihat limitasi di atas). DB apply blocked classifier → user jalankan migration. Belum dibangun (nunggu ACC design + user away).
  - **Cleanup:** file uji `test5s.jpg` (1x1, 333B) ke-upload ke folder Drive "Form 5S" prod saat verifikasi — perlu dihapus manual (removePhoto client belum trash file Drive; potensi orphan file saat user hapus foto — bug terpisah).
- **FIX sync folder ASPEK (2026-08-25) — PUSHED + EF DEPLOYED.** Gejala: file yang di-drop user LANGSUNG di folder Drive ASPEK (mis. "1.10 - ...") tak ke-sync ke app, malah rujuk file lain. AKAR: `syncAudit` cuma auto-tempel evidence dari folder `folder_type==="ITEM"` (terdalam); folder ASPEK jatuh ke jalur `recordUnassigned` (unassigned inbox). FIX: helper `folderSyncRole` (ITEM|ASPECT=evidence, KATEGORI ke atas=unassigned krn ambigu) + `aspectEvidenceMeta` (fallback itemId `${aspectId}::aspek`/itemLabel/categoryLabel utk folder aspek, no-op ITEM). App kelompokkan evidence per-`aspectId` saja jadi itemId placeholder aman. EF deployed self-host. Self-check `scripts/check-maturity-sync-classify.mjs`.
  - **FOLLOW-UP FIX (2026-08-25, deployed byte 47793):** (1) regresi — scanFolders role-based ikut nyapu folder FORM5S ke unassigned; `folderSyncRole` kini balikin `"skip"` utk FORM5S. (2) `SHEET_EXPORT_FOLDER_ID 1wh7A...` TERNYATA = folder UPT Surabaya maturity (yang di-scan) → Sheet hasil export muncul sbg unassigned tiap sync; kini skip mimeType `google-apps.spreadsheet` di jalur unassigned. 2 baris unassigned basi (test5s.jpg + sheet) dihapus dari DB. **GOTCHA belum tuntas:** folder output export = folder UPT scan; kalau mau bersih total, pindah output export ke folder di LUAR tree maturity (butuh folder baru dari user).
  - **BELUM SELESAI — evidence drop-manual di folder Drive:** user drop file di folder "1.10" versi manual (`1UQtA_...`, TANPA appProperties, tak terdaftar di `maturity_audit_drive_folders`) → sync tak lihat. Folder aspek 1.10 asli app = `1tzAMCm...`. Sync hanya scan folder terdaftar (app-created, ber-appProperties). Keputusan alur: user upload via app ATAU drop di folder app-created; folder manual duplikat tak didukung (adopsi folder manual = fitur terpisah, belum diputus).
- **Export Penilaian Maturity ke Google Sheet per-UPT — DIBANGUN + PUSHED (2026-08-25), sisa tes browser + deploy EF.** Lihat [[maturity-sheet-export-pending]]. Build hijau + self-check lulus.
  - **Impl:** tombol "Google Sheet" di `MaturityDashboardTab.jsx` (gate ADMIN/TL‖`canSwitchMaturityUpt`) → `exportMaturityGoogleSheet` (`useMaturity.jsx`) → `buildMaturitySheet` (`src/lib/maturitySheetExport.js`, **reuse template** `src/assets/maturity/matlev-template.xlsx`, isi sel nilai per-aspek) → EF action `export-sheet` (`maturity-drive`, upload folder `1wh7A...` + `mimeType google-apps.spreadsheet` auto-convert). TANPA tabel/skema/scope OAuth baru. Self-check `scripts/check-maturity-sheet-export.mjs`.
  - **KOREKSI seleksi sel (bukan warna kuning):** kuning `FFFF00` cuma highlight 4 baris dual-gudang (3.4/4.3/4.4/5.2), BUKAN penanda "harus diisi". Sinyal asli = **sel non-formula yang ADA di col 23 (Persediaan) / col 24 (MRWI)** per baris (tipe gudang tentukan kolom mana ada). Isi col 23/24 mana pun yang ada & bukan formula, nilai sama. GOTCHA: `XLSX.read(...,{sheetStubs:true})` WAJIB — tanpa itu formula `AVERAGE()` kategori/total kebuang senyap saat write. Nilai per-aspek integer 1-5; aspek belum dinilai → tampil default template (0), bukan kosong.
  - **EF DEPLOYED self-host 2026-08-25** (copy volume, byte identik 46414, curl 401 JSON "Tidak ada sesi login" = live; backup remote `index.ts.bak_export_*`).
  - **FIX FORMAT BERANTAKAN (2026-08-25):** tes pertama file hasil styling hancur — `XLSX.write` community edition gutting styles (`styles.xml` 29.627→1.114 byte). Diganti **zip-patch via `fflate`** (dep baru, disetujui arsitek): `unzipSync` template → resolve worksheet via workbook.xml→rId→rels (bukan hardcode sheet1) → `patchCell` regex tulis `<v>` di sel X/Y aspek (pertahankan `s=` style, skip self-closing & formula) → `zipSync`. Entri styling byte-utuh. Self-check assert `styles.xml` output >90% template. EF TAK berubah (frontend-only). **SISA cuma tes browser:** tombol → Sheet muncul di Drive, formatting mirip contoh + nilai benar; role non-writer ditolak 403.
  - **Tujuan:** sync nilai maturity dari app ke spreadsheet format baku, per UPT; awalnya **tombol manual** (user pilih manual, bukan auto-generate-saat-audit dulu).
  - **Path arsitektur (FINAL, tanpa blocker):** generate **`.xlsx` via SheetJS** (sudah dependency app) lalu upload via Drive API existing (konversi `mimeType: application/vnd.google-apps.spreadsheet` → Sheet live). **TIDAK pakai Sheets API / TIDAK butuh scope OAuth baru** (Drive scope cukup). Konsekuensi: update = regenerate seluruh file (bukan patch sel); styling berat SheetJS community terbatas (merge OK).
  - **Template acuan:** file `2026_Maturity Level Gudang_UPT_Surabaya_`, fileId `124INFjKQhJtWuVxVrDuPhe5k-NyekGjDwTDhFZcVQXQ`, folder output parentId `1wh7A1c96VYEjYJ10ZcCFwImyqJxPAThB`, owner `logistik.tjbtb@gmail.com` (= akun OAuth EF Drive). Naming output: `{tahun}_Maturity Level Gudang_UPT_{nama}_`.
  - **Struktur sheet:** matriks — baris = kategori + 30 aspek (1.1–5.4); kolom = No · Kategori · Level 1–5 (deskripsi) · Evidence · Keterangan · Tipe Gudang · **2 kolom nilai: "Gudang Persediaan…" & "Gudang MRWI/ATTB…"**.
  - **Sumber nilai:** `maturity_audits.aspekScores {aspekId:{upt,uit,pusat}}` → level efektif per aspek via `calcMaturityScore` (pusat‖uit‖upt‖rasio-evidence) di `useMaturity.jsx`. Format nilai contoh `3,4` (koma desimal ID) — KONFIRMASI apakah integer level 1-5 atau skor desimal.
  - **Mapping kolom gudang (keputusan user):** nilai aspek masuk kolom yang **sel-nya KUNING** di template — kuning = kolom itu diisi; kalau dua-duanya kuning → dua-duanya dapat nilai. **TODO implementasi:** peta "aspek → kolom kuning" TAK ada di NL-export (warna hilang); ekstrak via export sheet ke xlsx lalu baca cell fill (SheetJS), atau minta user daftar aspek MRWI.
  - **Fase 2 (disebut user di awal, ditunda):** auto-generate sheet format ini otomatis saat mulai audit, berlaku semua UPT.
- **MATURITY evidence + dashboard + Cari Foto — SELESAI + PUSHED (2026-08-24).** Semua build hijau.
  - **Fix upload evidence 400 (deployed self-host + push):** EF `maturity-drive` `parseJson(value)` fallback `{}` menghancurkan field string multipart (`auditId` "MA-..." → `[object Object]` → regex 400). Fix `parseJson(value, value)`. Upload foto+dokumen (accept sudah pdf/doc/xls/zip/dll) kini jalan. **EF sudah di-deploy ke self-host** (copy volume `functions/maturity-drive`).
  - **Autosave draft (push):** `autosaveMaturityDraft` di `useMaturity.jsx` — UPT (ADMIN/TL) isi evidence/nilai → autosave debounce 1.5s ke `maturity_audits`, status TETAP DRAFT, tanpa tutup-modal/toast/append-history, upsert idempoten by-id, concurrency in-flight+trailing. Induk (UIT)/Pusat lihat progres tanpa tombol. Isolasi per-UPT dijaga RLS existing (`can_write_maturity_upt`, autosave pakai JWT user). Indikator "Tersimpan otomatis". Tombol "Simpan Draft" manual TETAP (di overview, gate `canScoreUPT`=ADMIN/TL).
  - **Dashboard progres per-UPT (push):** `MaturityDashboardTab.jsx` — tabel lintas-UPT (progres tiap UPT, klik pindah, khusus `canSwitchMaturityUpt`) + 3 kartu detail (Progres per kategori / Sudah Diisi / Belum Diisi per-aspek) ganti blok hardcoded palsu. NOL-fetch (murni dari `audit.evidence` jsonb ter-load). `auditProgress()` helper.
  - **Mobile + apple-like:** direview — core sudah responsif (grid reflow, `.mobile-card-table`, 44px, skala font, radius). Fix nyata: select assign-evidence `fontSize 12→16`+`minHeight 44` HP (iOS zoom). Sisa polish minor dilewati (gold-plating).
  - **Cari Foto Data Stok 2 sumber (push):** `MiscModals.jsx` PhotoSearchModal — pecah 1 input jadi Kamera (`capture=environment`) + Galeri (tanpa capture).
  - **Lihat evidence via popup viewer scroll+zoom (push `6b1be46`, 2026-08-24):** klik file evidence di editor Maturity buka popup in-app (bukan download). Gambar = zoom −/reset/+ + scroll/pinch; PDF/lainnya = `<iframe>` (toolbar native browser). Reuse endpoint EF `download` (tanpa dependency/skema baru). 1 surface (`EvidenceViewer` di `MaturityAuditSystem.jsx`) nutup UPT/UIT/Pusat; drive & lokal keduanya lewat viewer. Lib `openMaturityDriveEvidence()` di `maturityDrive.js`. Download tetap ada di dalam modal. Self-check `scripts/check-evidence-viewer.mjs`.
  - **Sinkron evidence & folder Drive KONSISTEN + token-cache + sync cepat (push `77f7942`/`d41b209`/`5f28f28`/`2eafcaf`/`42a1037`, EF deployed self-host, 2026-08-24/25):**
    - **Tombol × evidence (`index.ts` action `unlink`):** kini konfirmasi popup (`askConfirmDelete` variant danger) + file Drive di-**trash** (`PATCH trashed:true`, 404 ditelan) + hapus baris `maturity_audit_drive_unassigned` → sync tak resurrect lagi (dulu `listChildren trashed=false` nemu file yang cuma soft-unlink di DB → `upsertEvidence` set `unlinked_at:null`).
    - **Folder Drive duplikat (2× "tenaga kerja"):** akar = upload multi-file `Promise.all` → `ensureFolder` cache-miss balapan → 2 folder appProperties-key sama. Fix: upload **sequential** (buang race) + `ensureFolder` >1 → `consolidateFolders` (pindah child ke folder keep, trash sisa) bukan throw + **heal saat `sync scanDrive`**. Heal awalnya per-folder (N query Drive/sync = LAMBAT) → **diganti `healDuplicateFolders` 1 query grouped** (`2eafcaf`): satu query semua folder appProperties `warnoto_maturity_root`, group by mapping key, merge dup scoped UPT/periode. Idempoten. Pure fn `chooseFolderMerge` + `mergeFolderGroup` + self-check `scripts/check-folder-merge.mjs`.
    - **Sync LAMBAT → cepat + blank/timeout hilang (`2eafcaf`+`42a1037`):** GEJALA user 2026-08-25: tekan "↻ Sinkronkan Drive" → app **blank putih + keluar menu** (akar = sync kelamaan → sesi 401 auto-logout, [[session-expiry-zombie-401]]) + error **"Permintaan Google Drive gagal."** (respons EF jadi HTML non-JSON = **timeout gateway** tunnel). AKAR lambat: `syncAudit` scan tiap folder Drive SERIAL lewat tunnel latency tinggi. FIX: (1) heal per-folder→grouped (di atas); (2) **paralelkan `listChildren`** semua folder via `mapLimit(folders, 8, ...)` — fetch Drive konkuren batas 8, tulis DB tetap serial (idempoten by `drive_file_id`). Guard `period` null (`periodKey: period?.key || audit.period_key`). Self-check `scripts/check-map-limit.mjs`. Blank SUDAH hilang (verified user); tinggal konfirmasi "Permintaan Google Drive gagal" hilang setelah paralelisasi. CATATAN: sync utama app (loadCloud / `loadMaturityDriveEvidence` scanDrive:false) TAK kena heal/scan — return sebelum blok itu.
    - **Presentase sinkron:** sync = 1 request tunggal, tak ada progres granular tanpa streaming (SSE) — DITUNDA; kalau perlu, pecah sync per-kategori di client + update % per batch (keputusan user setelah lihat kecepatan pasca-paralelisasi).
    - **Token-cache Drive (`driveToken`):** access-token di-cache scope modul + expiry (buffer 60s) — tadinya tukar refresh-token ke Google TIAP `driveFetch` (1 sync = puluhan round-trip). Efek: muat/tampil berkas cepat setelah request pertama panasin token; error "Token tidak dapat diperbarui" berkurang. Verified user: viewer PDF+gambar kebuka, "awalnya lama lalu cepat" (sesuai desain).
    - **CSP `frame-src blob:` (`vercel.json`, push `d41b209`):** CSP sudah ENFORCING (bukan Report-Only) tapi tak punya `frame-src` → iframe `blob:` (viewer PDF) kena `default-src 'self'` = diblokir ("logo sedih tiap halaman"). Fix tambah `frame-src 'self' blob:`. Gambar aman (`img-src` sudah `blob:`). Verified user.
  - **SISA verifikasi live (browser):** upload evidence foto+dokumen (Widi TL), autosave progres kelihatan di akun Erna (Admin UIT), dashboard tabel lintas-UPT, Cari Foto 2 tombol di HP. **(Viewer evidence + sinkron × / folder + token-cache SUDAH diverifikasi user 2026-08-24.)** Sisa spesifik sinkron: konfirmasi folder dobel LAMA benar-benar terkonsolidasi setelah "Sinkronkan Drive" pada audit yang terlanjur duplikat.

- **HARDENING KEAMANAN (asesmen whitebox) — 5 fix kode SELESAI + PUSHED (2026-08-18).** Plan
  `C:\Users\PLN\.claude\plans\saat-login-saya-mau-vivid-bear.md`.
  - **P2 XSS (`14e52c7`):** escape SEMUA field data di 13 print-builder `docBuilders.js` (+img src) pakai `esc()`. Live via Vercel. test `docBuildersXss`.
  - **P5 ADMIN kunci UPT (`14e52c7`):** `admin-create-user`/`admin-update-user` — ADMIN hanya kelola akun UPT sendiri (cek UPT lama DB+baru body), SUPERADMIN lintas-UPT. helper `bolehKelolaAkun`. test `adminScopeUpt`. **Perlu deploy EF.**
  - **P4 telegram secret (`14e52c7`):** `telegram-webhook` verifikasi `X-Telegram-Bot-Api-Secret-Token` vs env `TELEGRAM_WEBHOOK_SECRET` (fail-open+warn kalau env kosong). test `telegramWebhookSecret`. **Perlu deploy EF + set env + `setWebhook` secret_token.**
  - **P1 warnoto_state opsi A (`5278270`):** tulis pindah ke EF baru `sync-warnoto-state` (service_role, gate tolak VIEWER); klien `syncWarnotoState()` invoke EF. Migration DRAFT `20260818_warnoto_state_service_write.sql` (revoke write authenticated+anon, grant service_role, SELECT dipertahankan) **BELUM APPLY**. **URUTAN: deploy EF DULU, baru apply migration** (else window sync gagal). test `warnotoStateSync`.
  - **P3 header+SRI (`e9c15cd`):** `vercel.json` header enforcing (XFO DENY, nosniff, Referrer-Policy, HSTS 180d, Permissions-Policy `camera=(self)`) + **CSP Report-Only** (belum enforcing — amati violation dulu); SRI Leaflet unpkg@1.9.4 (hash terverifikasi). Live saat redeploy Vercel.
  - **Keputusan authz mengikat:** ADMIN TIDAK lintas-UPT (hanya SUPERADMIN); tulis `katalog`/`warnoto_state` idealnya lewat jalur terkontrol (P1 warnoto_state sudah; **katalog belum** — masih `authenticated write`, keputusan lanjutan opsi A untuk katalog PENDING).
  - **OPS SUDAH DIEKSEKUSI (2026-08-18):** 4 EF deployed ke self-host + verified 401-guard; migration P1 applied + verified (authenticated insert ditolak, service_role bisa, SELECT utuh); env `TELEGRAM_WEBHOOK_SECRET` diset di `.env` box + compose passthrough + container recreate + `setWebhook` secret_token — P4 enforcing (POST fake → 401). P5/P1/P4 semua ENFORCING di self-host. Backup `docker-compose.yml.bak_webhook_secret`.
  - **P1 KATALOG — opsi B SELESAI + APPLIED + verified (2026-08-18).** Migration `20260818b_katalog_write_role_gate.sql` (applied): policy write katalog jadi `role <> 'VIEWER'` via subquery profiles (bukan opsi A/EF — katalog ditulis banyak alur operasional sah, jadi B lebih aman & ringan). Predikat tunggal `bolehTulisKatalog` (roles.js) gate klien auto-sync di App.jsx + TUG15Tab (VIEWER tak push). Verified DB: VIEWER insert DITOLAK, non-VIEWER BISA, baca jalan. GOTCHA: edit TUG15Tab memicu tripwire test `tugCanonical` "TUG-15 report files outside canonical scope" (git-diff-quiet) — LOLOS begitu di-commit (bukan freeze permanen). DELETE katalog sudah efektif service_role-only (tak ada policy delete, pre-existing).
  - **OPS TERSISA (RESUME DI SINI — user pergi 2026-08-18):**
    - **(A) Cloudflare SSL Full(strict)** — belum diset user (dash.cloudflare.com → zona warnoto.com → SSL/TLS → Overview → Full (strict)).
    - **(B) CSP: sudah ENFORCING** (`Content-Security-Policy`, bukan Report-Only lagi — sudah di-flip). Allowlist aktif di `vercel.json`: `connect-src` (`wss://warnoto.com`+`*`, Supabase Realtime; `https://cloudflareinsights.com`; Sentry ingest), `script-src` (`unpkg.com`, `static.cloudflareinsights.com`, `wasm-unsafe-eval`), `img-src 'self' data: blob: https:`, `frame-src 'self' blob:` (**ditambah 2026-08-24 `d41b209`** untuk viewer PDF evidence — tanpa ini iframe `blob:` diblokir default-src). **GOTCHA fitur baru yang render iframe/embed/worker/koneksi origin baru WAJIB cek CSP dulu** (kalau diblokir → "logo sedih"/blank senyap). Kalau nanti ada resource sah kena blokir: tambah origin ke directive yang tepat di `vercel.json` lalu push. Belum dipastikan bersih total: Peta Gudang (tile OSM), scan kamera, cari-foto AI, cetak dokumen TUG — pantau Console kalau ada fitur gagal senyap.
    - **(C) LOW:** ~~proksikan `VITE_COHERE/GROQ/OCRSPACE`~~ SUDAH (Cohere+OCR via `services-proxy`, Groq via `ai-proxy`; nol key rahasia di bundle — item ini stale, 2026-09-02). ~~batas ukuran+sanitasi XLSX import~~ SELESAI (helper `src/lib/xlsxImport.js`: cap 15MB + `sanitizeCell/Rows` strip control-char+cap 5000, dipasang di semua titik `XLSX.read` termasuk `parseSAPFile`; self-check `check-xlsx-import-guard.mjs`; commit belum-push). ~~regen `schema.sql` drift~~ kolom `target` `maturity_audit_history` di-mirror ke schema.sql. SISA: verifikasi Cloud lama `tadxodrzoquugnsyejld` nonaktif.

- **2FA TOTP WAJIB SEMUA USER saat login — SELESAI + PUSHED + DEPLOYED (2026-08-18, `ed4df3a`).**
  Plan `C:\Users\PLN\.claude\plans\saat-login-saya-mau-vivid-bear.md`. Metode TOTP authenticator app
  (Google Authenticator/Authy), BUKAN email (login pakai email sintetis, tak ada SMTP). Factor disimpan
  GoTrue `auth.mfa_factors`, TANPA perubahan skema `public.*`, tanpa dependency npm baru. GoTrue self-host
  v2.189.0 MFA default ON (tak perlu ubah env container). **Gate di `handleAuthSession` (App.jsx)** sebelum
  `setCurrentUser`: `await mfa.getAuthenticatorAssuranceLevel()` → aal2=lanjut, aal1+nextAal2=challenge,
  else=enroll (auto fetch QR). Enroll pakai `issuer:"WARNOTO"` + `friendlyName=username` (tiap akun kebedaan
  di app authenticator; unenroll dulu factor unverified nyangkut). **Rollout:** `PROFILE_CACHE_KEY` v2→v3 +
  `setCurrentUser(null)` di cabang non-aal2 supaya cache-first tak bocorin UI AAL1. Audit LOGIN untuk
  `SIGNED_IN`|`MFA_CHALLENGE_VERIFIED`. UI langkah kedua reuse `loginSty`, input OTP numeric one-time-code,
  responsif `isMobile`. **Recovery admin:** Edge Function `admin-reset-mfa` (service_role, guard ADMIN/SUPERADMIN)
  DEPLOYED self-host (`/home/admin_warnoto/vps-dr-stack/volumes/functions/admin-reset-mfa/index.ts`, verified
  401-guard via kong localhost) + tombol "Reset 2FA" di Kelola Akun (gated `hasRole ADMIN`). **SISA:** verifikasi
  browser alur enroll→challenge→reset (review-first). **Edge minor non-blocking:** kalau `getAAL()` gagal
  transient (null) user aal2 jatuh ke enroll — risiko rendah (baca JWT lokal).
- **API INTEGRASI (SAP S/4HANA + app pihak ketiga) — FASE 1 SELESAI + LIVE + PUSHED (2026-08-17, `63e9803`).**
  Plan di `C:\Users\PLN\.claude\plans\task-notification-task-id-boyybm8kh-tas-crispy-tarjan.md`.
  **Edge Function gateway `integration-api`** LIVE di self-host (`https://warnoto.com/functions/v1/integration-api/<endpoint>`,
  deploy manual: copy ke `/home/admin_warnoto/vps-dr-stack/volumes/functions/integration-api/index.ts`, edge-runtime
  load per-request TANPA restart). Tabel `integration_api_keys` (hash sha256) + `integration_request_log`
  APPLIED di DB. Endpoint READ `/stock` `/catalog` `/tug` (API-key + scope + rate-limit), admin `/keys` `/revoke`
  (JWT **TL/SUPERADMIN** — ADMIN diturunkan). UI tab "Integrasi API", perm `aksi.kelolaApiIntegrasi`, docs
  `docs/INTEGRATION_API.md`. **Fase 1.5 HARDENING SELESAI + LIVE + TESTED (2026-08-17):** entropy key 256-bit,
  `expires_at`, `allowed_ips` (IP allowlist), `last_used_ip`, log SEMUA hasil auth (200/401/403/429). E2E test
  dari dalam box (bypass Cloudflare, `curl localhost:8000/functions/v1/integration-api/...`) LULUS 5/5: valid+scope
  200, wrong-scope 403, expired 401, IP-block 403, no-key 401; audit+last_used_ip terekam; key uji dihapus bersih. **VERIFIKASI BROWSER LULUS (2026-08-18):** login TL → tab Integrasi API render, buat key jalan, key tampil status aktif. **FIX GATING IZIN (2026-08-18, `098d572`):** dulu cabut `aksi.kelolaApiIntegrasi` di Matrix Izin tak sembunyikan menu (nav cuma cek `menu.integrasiApi`). Sekarang menu+tab (App.jsx) + server (`requireAdmin` edge fn hormati override `role_permissions`) semua cek `aksi.kelolaApiIntegrasi`; SUPERADMIN selalu boleh. Diverifikasi user: TL yang dicabut kehilangan menu. **Cloudflare Access sudah dipasang user & terbukti jalan** (reads `/stock` 403 tanpa service token; `/keys`+`/revoke` bypass, tetap dijaga JWT).
  **GOTCHA ROOT-CAUSE (fixed):** tabel self-host baru TIDAK otomatis dapat GRANT service_role → Edge Function kena
  "permission denied" senyap → semua request balas "API key tidak dikenal". Fix di `20260817c_integration_api_grants.sql`
  (grant service_role; anon/authenticated tetap terkunci). Migration kolom hardening di-apply via inline `-c`
  (baris `alter column drop not null` diskip — redundant, key_id sudah nullable). **SISA:** (a) ~~verifikasi browser~~
  ✓ SELESAI 2026-08-18 (TL buat key, status aktif); (b) **Cloudflare Access** service-token di depan `/stock`/`/catalog`/`/tug` +
  BYPASS di `/keys`+`/revoke` — user lapor "selesai C" (perlu konfirmasi scope benar biar panel admin tak mati);
  (c) **Prasyarat IT:** akses live SAP butuh `warnoto.com` diekspos ke jaringan SAP + IP egress SAP untuk allowlist.
  Fase 2 (ingest SAP→WARNOTO master, review-first) & Fase 3 (WARNOTO→SAP TUG) NUNGGU kontrak+kredensial tim SAP.

- **STOCK OPNAME — Fase 0, 1 & 2 SELESAI + DI-PUSH (2026-08-16..18).** Rencana 4-fase di
  `C:\Users\PLN\.claude\plans\pelaksanaan-esisting-stock-opname-warm-walrus.md` (+ eksekusi/audit di
  `...coba-buatkan-planningnya-dulu-snoopy-haven.md`).
  **⏳ FASE 1 & 2 DI-PUSH TAPI BELUM TES BROWSER USER** (build+audit+review arsitek lulus; verifikasi
  visual/fungsional ditunda — lihat Langkah berikutnya). **Fase 3 BELUM** (freeze peringatan).
  - **Fase 2 (`92b66d2`, mode hitung lapangan HP/tablet):** `OpnameLapanganView.jsx` BARU (blok picker+scan
    QR blok, item kartu per-blok, kartu hitung qty besar, dialog barang-tak-ada, tandai-nihil, antrian
    hitung ulang blind). `useHardwareScanner` opsi `blockInput`+`onScanStart` (blur field → char pertama scan
    tak bocor ke kolom qty). `BarcodeScanner` prop `continuous` + fallback iOS `@zxing/browser` (dynamic
    import, lazy chunk). Util QR blok `lokasiScanUrlFor`/`extractLokasiIdFromScan`(`?loc=`)/`buildLabelBlokHTML`.
    **Recount wajib** item selisih (blind, `applyQtyToItem` opsi `markRecount` — HANYA jalur lapangan;
    desktop `updateItem` TAK kena, perilaku Fase 1 utuh); `validate()` blokir submit selama recount pending.
    **Autosave localStorage** recovery (`warnoto_opname_draft_<id>`): restore saat sesi DRAFT dibuka, hapus
    HANYA saat `saveOpname` ter-sync server (`return true/false`) → offline-pending draft dipertahankan.
    audit-mobile 113 (<plafon 118).
  - **TEST E2E Fase 2 (`ac4601a`):** `tests/e2e/opname-lapangan.spec.js` (6 test @phone-360, HIJAU) —
    buka mode lapangan, recount blind wajib, recount blokir submit, burst scanner tak kotori qty
    (`keyboard.press` trusted), autosave pulih reload, + jalan keluar deadlock HP 100%. **FIX bug ditemukan
    test:** tombol "📱 Mode Lapangan" dulu desktop-only → HP di 100% dgn recount pending mentok (Submit
    terblokir tanpa jalan balik ke overlay); sekarang tampil juga saat `prog.pct===100`. **Fix test infra:**
    `fixtures.js` seed cache key v2→v3 (ikut rollout 2FA) — tanpa ini SEMUA spec E2E jatuh ke login.
    **Manual (perlu HP/scanner fisik, belum):** kamera continuous Android, fallback zxing iOS, HID KS-606,
    cetak Label QR blok. **PENDING pre-existing (di luar Fase 2):** `responsive.spec.js` 13 screenshot
    mismatch ~60px — baseline stale (kemungkinan sejak sebelum bump cache-key), perlu regenerate/investigasi.
  - **Fase 1 (model data, `sap.js`+`docBuilders.js`+`useStockOpname.js`+`StockOpnameTab.jsx`):**
    1a fix `normalizeKatalog` match PID (bug zero-padding); 1b `lokasiBreakdown` per item (dari
    `stocks`); 1c `hitungPerLokasi` per blok + `qtsFisik` turunan (`sumHitungPerLokasi` di sap.js),
    `approveOpname_Manager` tulis qty **per-lokasi nyata** (fallback `stockRows[0]` utk `_TANPA_LOKASI`,
    sesi lama tetap proporsional); 1d `saveOpname(opn,touchedLokasiIds)` **merge anti-timpa** —
    `mergeOpnameForSave` fetch versi server via `loadMasterTable("stock_opname")` (BUKAN CLOUD.get
    lokal; null=gagal→simpan lokal+toast, undefined=draft baru→simpan apa adanya); 1e sesi per gudang
    (`groupItemsByGudang`+dialog split, item tanpa lokasi→sesi "Belum Beralamat"); 1f filter Gudang/Blok
    + chip `BLOK(qty)` + `buildLembarHitungHTML`/`downloadLembarHitungHTML` (A4 Gudang→Blok+Paraf).
    Batasan diketahui (Fase 2): input qty desktop tunggal item multi-blok kolaps ke `_TANPA_LOKASI`;
    item lintas >1 gudang masuk sesi gudang pertama. Build hijau, audit 112.
  - **Fase 0** = satu layar: dropzone PID (`.csv/.xlsx/.xls`, +tombol "📂 Pilih File") ganti
    "+ Opname SAP"; sesi DRAFT dibuat SETELAH parse (cegah draft kembar); `activeTab` early-return
    dihapus → `renderPanel()` inline; bar aksi bertahap A/B/C (Submit hanya saat semua qty terisi);
    tombol "✕ Batal" eksplisit; ringkasan analisa 1 baris; sekat "Riwayat Opname" (hairline) pisah
    proses↔riwayat; chip filter compact pill.
  - **Konsistensi 2 menu:** StockCount kini pakai `OperationsHero` (banner sama Opname) + list
    flat/hairline seirama; gate upload StockCount & Opname → `can(...,"aksi.import")` (fix `hasRole
    ("ADMIN")` usang — TL dll kini bisa; `rolePerms` diteruskan ke kedua komponen dari App.jsx).
  - Mobile 360px dirapikan (flex-wrap header/riwayat, KPI auto-fit). Build hijau, audit **112**.
    Dead CSS `stockcount-header` (index.css) dibiarkan (harmless).
  - **Catatan (bukan bug kita):** 403 RLS `heavy_equipment_loans` saat loadCloud = cache loan basi
    sesi role lain di-push ulang (RLS benar menolak lintas-UPT); remedy = clear site data + login fresh.

- **MIGRASI DATA — izin matrix-driven + SAP import inline (2026-08-16, DI-PUSH).**
  (1) **Izin:** migrasi dulu hardcoded & mismatch (nav `hasRole("ADMIN")` di `AppSidebar.jsx` vs
  konten `hasRole("TL")` di `MasterDataTab.jsx`) → praktis cuma SUPERADMIN. Sekarang key baru
  `aksi.migrasiData` di `perms.js` (`PERM_AKSI` + default TRUE untuk TL/ASMAN/MANAGER/ADMIN_UIT/
  ASMAN_LOG_UIT/MGR_LOGISTIK_UIT/ADMIN_LOG_PUSAT), nav+konten pakai `can()` yang SAMA → TL bisa,
  admin atur per-role via Matrix Izin. auditLog subtab TETAP `hasRole("ADMIN")` (di luar scope).
  (2) **SAP import:** section "Cara lain/Legacy — SAP Langsung" dulu tersembunyi di `<details>` →
  jadi card biasa selalu tampil "📦 Import Data Stok (Template SAP)" sejajar card Template WARNOTO,
  note sumber = transaksi SAP `ZM_LAP_PERS_LOG` (sheet "UPT LAIN SAP"), apple-like (accent biru
  `C.accent`). Logika `handleSapLangsungFile` utuh. Build hijau, audit 114.

- **ATTB — modal edit material dirombak (2026-08-16, DI-PUSH).** Preview + edit LAMA (2 modal
  terpisah) dilebur jadi SATU `<section role="dialog">` 2-mode (`previewMode` view/edit) — tekan
  Edit ganti mode di modal yang sama (tak ada popup kedua); `editingId` dihapus. **2 slot foto
  wajib** (`fotoKeseluruhan`+`fotoNameplate`, validasi inline blokir simpan bila kosong, legacy
  `foto` disinkron `keseluruhan||foto||nameplate`). Mobile bottom-sheet (`operations.css` ≤700px,
  safe-area) + Web Interface Guidelines (Esc/focus-trap/focus-return/aria-modal/44px/overscroll-
  contain/unsaved-guard banner inline). Field edit ikut `previewFieldGroups` (tak ada field
  preview tanpa editor). Gotcha: validasi 2-foto memblokir edit record legacy tanpa nameplate
  (sesuai permintaan "wajib 2"). ATTB export = DITOLAK user (YAGNI); yang ada cuma Import (format
  sudah cocok akuntansi file2/file4, `docs/ATTB_SPEC.md` 7a/7b).

- **SCANNER HARDWARE Kassen KS-606 (keyboard-wedge/HID) TERINTEGRASI — DI-PUSH (2026-08-16).**
  Alat mengetik hasil scan + Enter; TANPA driver/SDK. Nol UI baru (tombol 📷 kamera tetap).
  - `src/hooks/useHardwareScanner.js` (baru): tangkap burst keydown (jeda antar-tombol < `gapMs`
    **120**) diakhiri Enter (`preventDefault`), panggil `onScan(kode)`. `onScanRef` cegah re-attach.
  - Reuse jalur scan yang ADA: TUG lewat `applyTxnScan(code,idx)` (di-refactor dari cabang
    `txnIndex` `handleScanResult`), Opname lewat `runOpnameScan` (scan=navigasi, qty tetap manual),
    Data Stok lewat `setSearch`. Di-arm per-menu (TUG saat `txnModal`, Opname saat form aktif,
    Data Stok saat tab mount). QR = `scanUrlFor(katalogId)`; `extractKatalogIdFromScan` parse sama
    persis dengan kamera. Lintas-platform (dongle USB desktop & Bluetooth HP) — sama, murni DOM keydown.
  - **Data Stok gotcha (scanner PELAN):** jeda antar-char > gapMs bikin hook gagal timing → URL
    numpuk mentah lewat ketikan native ke input fokus. Jaring pengaman: `useEffect` di `DataStokTab`
    resolve `?scan=<katalogId>` → no.katalog (matchesStockSearch cek `stock.katalog`/`id`, BUKAN
    katalogId), idempoten.
  - **TUG anti-salah-pilih (safety):** no.katalog TIDAK unik (kunci paket 5-tuple MARA) + 1 material
    bisa banyak lokasi → `applyTxnScan` >1 match TIDAK auto-pilih lagi, buka `ScanPickerModal.jsx`
    (baru) untuk user pilih material+lokasi. 1 match=isi langsung, 0=toast error.
  - **Keputusan arsitektur (jangan diulang):** registrasi/penolakan alat scan spesifik TIDAK bisa
    di web keyboard-wedge (keydown nol identitas device); WebHID merusak alur BT-HP & cuma kenal
    model bukan unit. Batas keamanan = auth user + scope UPT/RLS + approval, BUKAN alat. Isolasi
    1-catalog-lintas-UPT sudah dijamin login-scope (`stocks` per RLS), bukan scanner.

- **ROMBAK BESAR UI MOBILE — Gelombang 0 & 1 SELESAI + DI-COMMIT + PUSH (2026-08-16).**
  Arah: perbaiki keterpakaian + polish minimalism Apple-like di atas token yang ada
  (BUKAN bahasa desain baru). Kontrak ditulis di `docs/DESIGN_GUIDELINES.md` seksi 17
  (CSS-first satu sumber layout; baseline modal responsif global; arah polish Apple-like).
  - **Gelombang 0** (`7bd2462`): baseline modal responsif global di `index.css`
    (@media ≤768 — overlay center jadi top-align + `overflow-y:auto` + safe-area, panel
    `margin:auto`; fix modal kepotong tanpa scroll, nol edit JSX). Re-baseline SELURUH
    screenshot Playwright HP/tablet — baseline lama basi karena Fase 1-5 tak pernah di-commit
    (ini menuntaskan "Fase 6 verifikasi browser" yang dulu pending). Utility grid KPI global
    dibatalkan (YAGNI: mayoritas sudah `repeat(auto-fit,minmax)` auto-wrap).
  - **Gelombang 1** (`f371991`): (a) Approval — baris `:has(> .approval-actions)` di-stack
    kolom di HP (konten penuh + tombol di bawah), fix teks terjepit. (b) Data Stok redesign
    kartu HP per keluhan user: tombol scan 44×44 simetris (`.stock-scan-button`), foto
    44→60px, padding 12→14px, chevron "Detail ›" eksplisit (tap buka StockDetailModal, terbukti
    test), badge generik disembunyikan khusus `.stock-card-table`. 2 file (`DataStokTab.jsx`,
    `stock.css`), presentation-only.
  - Verifikasi tiap gelombang: scanner `audit-mobile.mjs` **118** (lantai terjaga), `npm run build`
    hijau, `mobile-minimal` 3/3 + tap-modal test lulus, review-first (user OK visual) sebelum commit.
  - **Gelombang 2+ (2026-08-16, commit lanjutan):** Dashboard Eksekutif "Overview Gudang"
    di-declutter (redundansi nilai Rp dibuang, KPI card diseragamkan jadi `.kpi-card`, label
    KPI wrap bukan ellipsis); Analitik Material 3 widget diseragamkan (baris hairline, badge
    tenang, urgensi via warna teks); Kapasitas KPI banner grid dirapikan (item ke-7 full-width).
    Rekomendasi Pengadaan (`forecastView==="procurement"`, tabel `ForecastStokPage.jsx:368-398`):
    inline `maxWidth:120/180` pada td bocor ke kartu HP bikin kolom Merk jatuh vertikal —
    fix global `.mobile-card-table__row > td { max-width:none !important }`. **Badge "Ketuk
    untuk detail" generik DIHAPUS global** (`content:none`, index.css) atas permintaan user
    (kartu tetap bisa diketuk; Data Stok tetap punya chevron "Detail ›" tersendiri).
    Admin/master/config + Alat Berat sudah diaudit = bersih, tak di-churn.

  ### Checklist uji tampilan mobile (per 2026-08-16)
  LOLOS visual 360px (ACC user): Data Stok (user: "lumayan baik"), Forecast Stok, Approval,
  Dashboard Eksekutif/Overview, Analitik Material, Kapasitas Gudang, Master Organisasi/Migrasi,
  Permission Matrix, Alat Berat, Pak War/Telegram config, baseline modal (Material detail),
  Rekomendasi Pengadaan (merk & susunan) + Maturity (UPT dropdown) [user ACC "lumayan ok"
  2026-08-16], Master Gudang [user ACC 2026-08-16 — lolos kontrak responsif 360px + wrap badge
  baris blok defensif untuk data padat].
  LOLOS kontrak e2e 360px, BELUM ACC mata (fixture kosong / tak render populated):
  Peta Gudang (grid collapse + tooltip clamp sudah ada; select toolbar difix full-width HP —
  responsive.spec PASS; perlu cek device dgn denah terupload).
  VERIFIED overflow-safe by inspection (2026-08-16, bukan bug): modal besar Kartu Gantung Digital
  (panel maxWidth:100%+scroll, tabel riwayat dibungkus overflowX:auto, grid Foto/QR 2-kol muat 328px
  = fidelitas dokumen TUG.2 sengaja) + Cetak Barcode (chip flexWrap, semua baris muat). Tak ada fix
  overflow perlu ditulis; polish stack Foto/QR ditolak (ubah layout kertas cetak). Sisa: cek device.
  DIKERJAKAN + COMMIT+PUSH sesi 2026-08-16 (lanjutan):
  - Network Overview "Overview Gudang" (`95eeead`): card-table murni `.network-card-table`
    (buang aturan fixed-table bespoke yg bikin angka "644" pecah vertikal + Status hilang),
    badge "Belum aktif" 1 baris, buang tabIndex expand-acak. + polish apple-like DashboardManager
    (bobot 900→700, tracking, shadow halus, breathing). + Peta Gudang select full-width HP +
    Master Gudang wrap badge Daftar Blok.
  - Stock Opname & Stock Count mobile (`81c96b7`): tabel editable Opname → card-table per-item;
    tombol Upload Step-1 SAP fix (label inline padding menimpa judul → display:flex full-width);
    declamp teks Format/help (tak terpotong). Stock Count draft card-table ringkas (checkbox
    __photo, hide No Katalog/Rekomendasi HP), header stack + tombol upload full-width, banner
    dipersingkat+declamp. User ACC: Opname "lumayan", Count rapi setelah 2 putaran.
  ✅ ATTB ringkasan per-UPT — SELESAI (2026-08-16, gate dibuka user): breakdown per-UPT aditif
  di `AttbDashboardSummary.jsx` untuk viewer UIT/MSB saat >1 UPT (Item/Nilai Buku/Est. Lelang/Tertahan,
  sort Nilai Buku desc); viewer 1-UPT tak berubah. Build hijau, audit 114.

  ### ⏳ PENDING lanjut (2026-08-16, untuk sesi rumah)
  1. ✅ **Baseline visual e2e di-refresh (2026-08-16)** — `npm run test:responsive:update`, 9 PNG
     re-baseline (Dashboard-detail, ATTB-pipeline, Stock, Stock-count, TUG-15 di phone 360/390/412
     + tablet 768), cek per-file semua map ke surface mobile wave, committed+push.
     Gate asli tetap: `scripts/audit-mobile.mjs` (114) + build + review browser.
  2. **Cek device (bukan e2e stub) — tugas USER, bukan kode:** Peta Gudang (butuh denah terupload;
     overflow sudah aman by-inspection, keputusan user 2026-08-16 cukup device-test tanpa seed fixture),
     Rekomendasi Pengadaan, Maturity UPT dropdown, modal besar Kartu Gantung/Cetak Barcode (opsional).
  3. ✅ **ATTB ringkasan per-UPT — SELESAI (2026-08-16)** (lihat entri di atas).
  4. Backlog lama tetap: `python ml/train_forecast.py` (env service_role/CI, non-blocking);
     fitur baru MTU KHS & gudang MRWI (belum didesain); Non-SBY UPT isi `minQty`/histori (data gap).

  ### Pending jangka pendek rombak mobile (urut prioritas)
  1. ✅ Verifikasi device Rekomendasi Pengadaan + Maturity UPT dropdown — user ACC "lumayan ok" 2026-08-16.
  2. ✅ Peta Gudang mobile — audit selesai: overflow sudah aman (grid collapse + tooltip clamp);
     select toolbar difix full-width HP (`.warehouse-map-toolbar__field select`). responsive.spec PASS.
     Sisa: cek device dgn denah terupload (tak render di fixture e2e). ✅ Master Gudang juga difix
     (wrap badge baris blok) + PASS.
  3. ✅ Modal besar Kartu Gantung Digital & Cetak Barcode — audit selesai: overflow-safe by
     inspection (tak ada fix perlu). Sisa: cek device (opsional).
  3b. ✅ ATTB ringkasan per-UPT — SELESAI 2026-08-16 (gate dibuka user, breakdown aditif).
  5. Alat Berat mini-grid di dashboard — polish warna (opsional, dipakai 3 dashboard).
  6. Kapasitas — konfirmasi user apakah mau hierarki ukuran metrik primer (m²/%) vs count.
  7. Uji crowding data nyata (nama panjang/banyak baris) yang tak muncul di e2e stub.
  Pola kerja: user tunjuk screen → audit PNG → fix root-cause → re-baseline → commit+push.

- **MODE RINGKAS HP SELESAI + TERVERIFIKASI DI BROWSER — DI-COMMIT (`93abdf0`, mobile wave 2026-08-16).**
  Verifikasi lewat Playwright 360px (`npx playwright test tests/e2e/mobile-minimal.spec.js --project=phone-360`,
  3 test lulus) plus pemeriksaan screenshot mentah. Empat cacat ditemukan dan diperbaiki saat verifikasi:
  (1) selector penanda "Ketuk untuk detail" invalid karena `:has()` bersarang di `:has()` — seluruh aturan
  dibuang browser, penandanya tidak pernah muncul; (2) `.info-note` salah dipasang di opsi dropdown
  `SearchableSelect`; (3) `.info-note` dipasang di dalam `<details>` ForecastStokPage (dobel disclosure);
  (4) `scripts/check-card-collapse.mjs` diperluas supaya menangkap `:has()` bersarang. Empat kegagalan
  non-screenshot di `responsive.spec.js` sudah ada SEBELUM pekerjaan ini (terbukti lewat run di HEAD bersih)
  — utang fixture E2E, bukan regresi. Permintaan user: tampilan material di Forecast Stok > Material Cadang terlalu panjang, banyak tulisan panjang di Forecast Stok & Pak War versi HP; referensi rasa Apple / SaaS enterprise untuk semua menu. Aturannya jadi `docs/DESIGN_GUIDELINES.md` bagian **15**.
  - **Kartu tabel ringkas** — kartu di HP hanya menampilkan **4 sel pertama + sel bertombol**; sisanya terbuka saat kartu disentuh (baris dengan modal detail lewat onClick, tanpa modal lewat `:focus`). 28 baris kartu diberi `tabIndex={0}`. Tabel Material Cadang yang tadinya 14 baris per kartu kini 5. Kolom Status ditandai `.is-key` supaya tetap tampil. Nama material dipotong satu baris sampai kartu dibuka.
  - **Kalimat penjelas disembunyikan di HP** (bukan dikecilkan): `<small>` di `.kpi-banner`/`.forecast-overview__copy`/`.approval-section-title`/`.exec-overview__heading`/`.forecast-cockpit-col-title`, `<p>` di `.approval-hero`/`.maturity-hero`, dan teks sekunder halaman Pak War (identitas, eyebrow, status, pertanyaan lengkap di tombol pertanyaan cepat). `<small>` berisi angka/tanggal sengaja dibiarkan.
  - **Varian tombol** — pemetaan ke lima varian `sty.btn` selesai, tapi **cakupannya jauh lebih kecil dari dugaan awal**: dari 579 tombol, 300 sudah `sty.btn`, 187 memakai class yang sudah seragam, 36 `transparent` (ikon), ~30 toggle segmented, 19 polos. Yang benar-benar menulis warna varian sendiri hanya **6** — semuanya sudah dipindah (5 close-button ke `sty.btn("danger","sm")`, 1 hijau `#10b981` ke `C.green`). Angka "261 tombol di luar token" di laporan sebelumnya **keliru** — itu menghitung tombol berbasis class yang sudah konsisten.
  - Penjaga regresi baru: `node scripts/check-card-collapse.mjs` (aturan `:focus` pembuka kartu harus lebih spesifik daripada aturan penyembunyi — sempat salah sekali).
  - Build hijau, skor scanner responsif tetap **118**.
- **KONSISTENSI DESAIN SELESAI — DI-COMMIT (`93abdf0`, 2026-08-16); verifikasi browser tercakup mobile wave.** Lanjutan dari keluhan user: bentuk tombol, warna teks, ukuran font tidak seragam, dan banner berisi paragraf terlihat aneh di HP. Aturannya jadi `docs/DESIGN_GUIDELINES.md` bagian **11 (skala tipografi), 12 (bentuk tombol & sudut), 13 (banner .info-note), 14 (palet warna teks)**.
  - Tipografi — 21 ukuran inline jadi **7** (12/13/15/17/20/24/32); 161 nilai inline + 35 nilai CSS dibulatkan; `sty.label` 11→12 (lantai 12 px), `pageTitleStyle` 22→20. 16 px tetap di input (cegah zoom iOS). Codemod `scripts/typescale.mjs`.
  - Tombol — `sty.btn` `fontWeight` 600→700; `.table-action-button` sudut 7→10 & bobot 750→700; `.approval-actions button` bobot 800→700; 22 tombol ber-inline-style disamakan. Ketiga sumber bentuk tombol kini identik.
  - Sudut — skala tiga nilai **10 / 14 / pill**; 304 nilai inline + 88 nilai CSS dibulatkan. Codemod `scripts/radiusscale.mjs`.
  - Warna teks — 45 warna unik jadi **32**; keluarga abu & biru dipetakan ke `C.muted`/`C.accent` (46 penggantian). Teks di atas latar bertint sengaja dibiarkan.
  - Banner — **49 banner berparagraf** ditandai `.info-note` + `tabIndex={0}`. Di HP hanya 2 baris pertama tampil, sisanya terbuka saat disentuh (CSS `-webkit-line-clamp` + `:focus`, tanpa JavaScript). Desktop utuh, lebar baca 72ch.
  - Build hijau, skor scanner responsif tetap **118** (tidak ada regresi).
- **RESPONSIF MOBILE FASE 1-5 SELESAI — DI-COMMIT (mobile wave 2026-08-16); verifikasi browser tuntas via re-baseline e2e 9 PNG.**
  - Rencana: `docs/MOBILE_AUDIT_2026-08-15.md`. Aturan hasilnya: `docs/DESIGN_GUIDELINES.md` bagian **8 (pola tabel), 9 (breakpoint), 10 (lantai skor)**.
  - Fase 1 — 19 tabel dikonversi ke `.mobile-card-table` (CSS generik lama, **tidak ada CSS baru**), lewat codemod `scripts/cardify.mjs`. 9 tabel sisa = daftar kecuali resmi.
  - Fase 2 — `minWidth:0` disisipkan di 25 elemen ber-ellipsis; dua aturan CSS baru: anak sel kartu ikut membungkus, dan baris ber-tombol-banyak (`:has(> button + button)`) di-`flex-wrap` supaya floor 44px tidak memaksa lebar melebihi layar.
  - Fase 3 — grid `minmax(260/280/300/320px,1fr)` dipaksa satu kolom di HP lewat satu aturan CSS (`[style*="minmax(..."]`, konvensi yang sudah dipakai file ini).
  - Fase 4 — 25 `fontSize` <12 px dinaikkan ke 12; layout cetak sengaja dilewati.
  - Fase 5 — breakpoint 7 tier jadi **520 / 768 / 1024** (+ `min-width:769`). **Menyimpang dari rencana 640**: 768 dipakai supaya CSS sejajar dengan `isMobile` (`innerWidth<=768`); 768/1024 = `md:`/`lg:` Tailwind.
  - Skor scanner (detektor diperbaiki, baseline dihitung ulang dari HEAD): **261 → 118**. Sisanya lantai permanen, lihat guidelines bagian 10.
  - **Fase 6 (verifikasi browser 360px) SELESAI** — dituntaskan Gelombang 0 mobile wave (lihat entri teratas) + re-baseline e2e 9 PNG (2026-08-16).
- **PENDING ITEM (per akhir sesi 2026-08-14 — semua kode ter-commit & push, tree bersih).**
  - **Jangka pendek:**
    1. **`war-uimobile` rename tertunda** — `.claude/skills/uimobile` → `war-uimobile` gagal (folder ke-lock file handle Windows, ada proses cwd di folder itu, bukan shell agent). 4 skill custom lain SUDAH di-prefix `war-` (`c968c61`). Fix: tutup editor/explorer/terminal yang buka folder itu (atau restart), lalu `git mv .claude/skills/uimobile .claude/skills/war-uimobile` + sed frontmatter `name: war-uimobile` + `/uimobile`→`/war-uimobile` + rename juga `.agents/skills/uimobile`.
    2. **Jalankan `python ml/train_forecast.py`** (env Supabase service_role / GitHub Actions) → prediksi ML per-UPT terisi. Leak lintas-UPT SUDAH tertutup (migration+filter); ini untuk akurasi ke depan. Tak blocking.
    3. **Verifikasi alur upload Material Cadang baru** default UIT-scope (read sudah verified: Gresik lihat analisa UIT-JBM).
  - **Jangka panjang:**
    1. **[FITUR BARU] Sinkron data MTU KHS** — belum didesain/dibangun. (definisikan sumber & mapping saat mulai.)
    2. **[FITUR BARU] Sinkron data gudang MRWI** — belum didesain/dibangun. (definisikan sumber & mapping saat mulai.)
    3. **Non-SBY UPT isi metadata operasional** — `minQty` (stok minimum) semua 0/kosong di 5 UPT non-SBY → Forecast Kritis & Rekomendasi Pengadaan mereka kosong (bukan bug, data gap). Perlu admin UPT isi minQty (+histori transaksi +analisa Material Cadang). Opsi: tambah kolom minQty di template import biar massal.
    4. **Importer Template Migrasi Stok full-stock** di menu Migrasi Data (belum dibangun, lihat bagian "Lain").

- **Sesi 2026-08-14 (Opus) — Forecast Stok ML isolasi per-UPT + rapikan Master Katalog. DI-PUSH (`b484180`, `fdca211`).**
  - **Forecast ML per-UPT (`b484180`):** `forecast_predictions` dulu tanpa `upt_id` → prediksi Prophet (praktis cuma SBY punya histori) bocor ke UPT lain yang menstok katalog sama (26 katalog). Migration `20260814_forecast_predictions_upt_id.sql` (kolom `upt_id`, unique `(katalog_id, upt_id, tanggal_prediksi)`, backfill lama→UPT-SBY) **APPLIED self-host**. `ml/train_forecast.py` grup `(upt_id, katalog_id)` (derive upt via `lokasi_id→gudang.upt_id`; baris legacy lokasi-diarsip 97/104 fallback UPT-SBY; current-qty per-UPT dari `stocks.upt_id`). `ForecastStokPage.jsx` query filter `.in("upt_id", dataScope)`. Verified: scope Gresik=0 prediksi (leak tutup), SBY=1440. **SISA (ops, tak blocking):** jalankan `python ml/train_forecast.py` (env service_role/CI) untuk akurasi per-UPT ke depan; leak sudah tertutup tanpa itu.
  - **TEMUAN DATA (bukan bug kode):** Forecast/Material Cadang/Rekomendasi Pengadaan "sama/kosong" di UPT non-SBY = **data gap, BUKAN gagal scoping**. Scoping stok terverifikasi benar (Fajar/SBY 266 vs superadmin/nasional 527). Non-SBY UPT baru masuk qty stok tapi **belum isi metadata operasional**: `minQty`=0/kosong semua (→ tak ada "di bawah minimum" → Rekomendasi Pengadaan kosong), tak ada histori transaksi (→ ML "histori belum cukup"), `material_cadang_state` cuma SBY. SBY satu-satunya UPT data matang.
  - **Master Katalog (`fdca211`) + trigger satuan MARA (`2138b7a`) + GRANT anon (`da6b216`)** — lihat entri di bawah / commit.
- **Sesi 2026-08-14 (Opus) — housekeeping pending item.** (1) `sap_foto_report.json` (artefak generate `fill_sap_photos`) di-`.gitignore`, tidak di-commit. (2) **Bot Telegram: fase-2 scoping (`p_upts`) TERNYATA SUDAH DEPLOY di self-host sejak Aug 9** (bukan "belum deploy" — HANDOFF lama basi); env box lengkap (TELEGRAM/GROQ/COHERE/SERVICE_ROLE). (3) **GAP kebocoran state nasional DITUTUP** (`telegram-webhook/index.ts` L360-364): blob `warnoto_state` (top20ByValue/materialKritis, tanpa penanda `upt_id` per item) dulu disuntik ke SEMUA user termasuk scoped-UPT — untuk mereka bocor lintas-UPT DAN salah (bukan data UPT-nya). Fix 1 baris: `callerUptId ? Promise.resolve("") : buildWarnotoStateContext()` — hanya user global (callerUptId=null) yang disuntik state nasional; scoped-UPT tetap dapat RAG chunk mereka (sudah scoped `p_upts`). Restruktur `buildWarnotoStateSnapshot()` App.jsx untuk state per-UPT = DITUNDA (YAGNI). **GAP fix SUDAH DEPLOY & live-healthy di box** (verified). (4) **Satuan 21 material non-stock — RESOLVED via TRIGGER DB** (migration `20260814_katalog_satuan_from_mara_trigger.sql`, applied prod). AKAR: fill `katalog.data->>'satuan'` via SQL langsung diklobber full-sync app (`saveToCloud`) dalam menit — `katalog` dimiliki app-state. Fix app-layer rapuh (banyak write-path + semua client harus refresh). Solusi: trigger `BEFORE INSERT OR UPDATE ON katalog` (`katalog_fill_satuan_from_mara`) isi satuan dari `mara_catalog` (`ltrim(kode,'0')`) saat KOSONG — menangkap tulisan app/bot/edge/import, non-destruktif (satuan terisi tak disentuh). +functional index `idx_mara_kode_ltrim`. Backfill 45 katalog kosong. **Uji clobber LOLOS:** kosongkan satuan lalu baca = terisi ulang otomatis. 20 premem: 0 kosong (17 BH, `3061803`+`6020126`=SET, `1002070458`=U). App sync tak bisa lagi mengosongkan satuan. (5) **GRANT anon dipertegas** (migration `20260814`): cabut SELECT vestigial `warnoto_state`/`wa_sync_status`/`tg_agent_logs`, `katalog` scan-QR utuh; tulis anon sudah nol. (6) Verifikasi dropdown scope form Kelola User Bot = user konfirmasi OK. (7) whitelist `warnoto.com` kantor = CLEAR. **SISA commit ke repo:** `telegram-webhook/index.ts`, `.gitignore`, `supabase/migrations/20260814_*.sql`, HANDOFF. GAP fix & migration sudah live di box, belum di git.

- **Sesi 2026-08-11 (Opus, sore) — rombak menu Data Stok jadi 2 lapis (ringkas → detail). BELUM DI-COMMIT, build hijau, menunggu review browser user.**
  - **Rencana 4 batch** (disetujui user): B1 ringkas tampilan awal · B2 sorting+filter gudang/blok+chip · B3 toggle agregasi per-katalog vs per-lokasi · B4 scan barcode di search + polish (highlight, sticky header, empty state). B5 (nilai stok, qty dibooking, umur/dead-stock, export) = keputusan terpisah, belum disetujui.
  - **B1 SELESAI:** tabel `DataStokTab.jsx` 9-10 kolom → **5 kolom** (Foto/Nama Barang/Qty/Lokasi/Aksi), `minWidth` 980→640. Kolom Kategori/Harga/Status/Lokasi UPT + 2 `<select>` inline dibuang dari tabel. Select Gudang+Blok pindah **verbatim** ke `src/components/PindahBlokModal.jsx` (file baru, dipicu ikon di kolom Aksi, ADMIN/TL). Edit/Hapus pindah ke modal detail (`App.jsx` ~L4023). Modal detail +field Sub Gudang & Lokasi UPT. Header pakai `OperationsHero`.
  - **Gotcha `OperationsHero`:** slot `controls` dikunci **230px** (`operations.css` L36/L94, grid `230px minmax(0,1fr)`) — dirancang untuk SATU `<select>` sempit (pola Alat Berat/ATTB). Menaruh search+tombol+3 filter di situ bikin terjepit. Search/filter HARUS di `workspace-filter-panel` di bawah banner; `operations.css` jangan diubah (dipakai Alat Berat & ATTB).
  - **Gotcha scope banner:** viewer 1-UPT punya `stockUptFilterOptions` KOSONG (dropdown UPT memang hanya untuk Pusat/UIT), jadi fallback ke "Semua UPT" itu salah — pakai `currentUptNama` (App.jsx L3286).
  - **B2 SELESAI:** state `stockGudangSelect`/`stockBlokSelect`/`stockQuickFilter`/`stockSort` + sort/filter gudang-blok/chip aktif. `sortBlokOptions` di-dedup ke `src/lib/masterSync.js` (dipakai `PindahBlokModal.jsx` & `DataStokTab.jsx`). Fix: `kritisCount`/`tanpaLokasiCount` sekarang dari `scopedEnrichedStocks`, bukan `stocks` mentah.
  - **Revisi B2 SELESAI:** kolom Nama Barang dirapikan (max 3 baris, dot status SAP bukan pill, chip "Menunggu approval" gabungan). `src/lib/sap.js` `resolveSapLabel`/`stockSapLabel` sekarang terima override eksplisit "SAP — Persediaan"/"SAP — Cadang" yang menang atas heuristik `STK-PREMEM-*`; form Edit (`StockModals.jsx`) punya field "Status Material".
  - **B3 SELESAI:** toggle "Per Lokasi"/"Per Katalog" (`stockViewMode`, App.jsx dekat `stockSort`). Mode katalog = `viewStocks` hasil `Map`-groupby `katalogId` (baris sintetis `id:"AGG-"+katalogId`, qty/minQty dijumlah, `lokasiCount`+`aggMembers` disimpan) disisipkan antara `filteredStocks` dan `sortedStocks` (App.jsx ~L3380). Mode "lokasi" (default) `viewStocks===filteredStocks` apa adanya — tidak ada perubahan perilaku. Klik baris agregat / tombol "Lihat lokasi" → balik ke mode lokasi + `search` diisi no. katalog (bukan modal, baris sintetis tidak boleh masuk `stockDetailId`). Tombol "Lihat di Peta Gudang" (mobile+desktop) di-guard `!isAgg` (baris agregat tidak punya `lokasiId`).
  - **B4 SELESAI.** Sticky `thead th` tabel Data Stok (`stock.css`, scoped `.stock-card-table`, warna solid `#0b2559`/dark `#0b1830` sesuai `C.sidebar`). Highlight kata kunci di Nama Barang+no.katalog (`<mark>`, helper lokal `highlightText` di `DataStokTab.jsx`, regex-escape wajib, skip kalau `search.trim()<2`). Scan barcode di search bar reuse mekanisme global `openScanner({onDetect})` (App.jsx, pola sama `StockOpnameTab.jsx`) — TIDAK ada file/komponen scanner baru, cuma prop `openScanner` diteruskan ke `DataStokTab`. Shortcut "/" fokus search (`useEffect`+`ref`, abaikan saat sedang mengetik di field lain), placeholder "…(tekan /)". `useDeferredValue(search)` (React 18.2.0 terpasang) dibungkus di App.jsx sebelum masuk `filteredStocks` biar input tetap responsif.
  - **REVISI 3 SELESAI (keluhan modal berlapis + toggle nyasar), tinggal review browser user.** Modal detail Data Stok (App.jsx ~L4016) digabung jadi SATU pop-up 2-mode pakai state `stockModal` yang SUDAH ADA (`"edit"`=form, `null`=view read-only) — tombol Edit tidak lagi `setStockDetailId(null)` lalu buka modal lain, cukup `openEditStock(st)`, modal detail tetap terbuka. Field form diekstrak dari `StockDetailModal` (dead code, dihapus) jadi `StockEditFields` di `src/components/StockModals.jsx`, dipakai inline saat `stockModal==="edit"`. `saveStock`/cabang approval `fieldsChanged && !isTL` (App.jsx ~L1955) TIDAK disentuh — cuma dipicu lewat `stockModal` seperti sebelumnya. Tutup pakai icon `X` (bukan tombol merah kotak), header+footer sticky (reuse `sty.modalHeader`/`sty.stickyFooter`), mobile jadi bottom sheet (`alignItems:"flex-end"`, radius atas saja, `env(safe-area-inset-bottom)`), field read-only dikelompokkan 2 seksi (Identitas / Lokasi & Stok). ESC+fokus balik ke trigger via `stockDetailModalRef`/`stockDetailTriggerRef` (App.jsx dekat deklarasi `stockDetailId`) — ESC saat edit balik ke view dulu, klik overlay cuma menutup saat view. Toggle "Per Lokasi/Per Katalog" pindah dari bawah search ke toolbar kanan-atas tabel, prefix "Tampilkan:" + `title` per tombol. Prop `openEditStock` yang jadi yatim di `DataStokTab` dibuang (prop `deleteStock` juga yatim tapi PRE-EXISTING dari refactor lama, sengaja tidak disentuh — di luar scope revisi ini).
  - **REVISI 4 SELESAI (prinsip "maks 1 lapis overlay"), tinggal review browser user.** Modal detail Data Stok (App.jsx ~L4030) dapat tab "Detail"/"Riwayat" (state baru `stockDetailTab`, reset ke `"detail"` tiap ganti `stockDetailId`; disembunyikan saat `stockModal==="edit"`) pakai `.operations-segments` di dalam wrapper sticky yang sama dengan header (`position:"sticky",top:-20`). Tab Riwayat reuse `buildKartuGantungHistory` (`src/lib/sap.js`, TIDAK dihitung ulang) → dirender kartu (bukan tabel), urutan dibalik sekali (fungsi return lama→baru), dibatasi 20 + tombol "Tampilkan semua (N)" (state `riwayatExpanded`). Tombol "Lihat Kartu Gantung" di badan Detail dibuang → jadi tombol footer "Cetak Kartu Gantung (TUG-2)": desktop `closeModal()` lalu `setKartuGantungDetail(kat)` (batched, tidak pernah 2 overlay bareng); mobile langsung `buildTUG2FrontHTML`+`window.open` tanpa preview, fallback `showToast` kalau popup diblokir. `KartuGantungModal.jsx` TIDAK disentuh. Guard unsaved-changes mode edit: snapshot `stockFormSnapshotRef` diambil di `openEditStock` (App.jsx ~L1918), `stockFormDirty` = `JSON.stringify` compare vs `stockForm`; Batal/ESC/X saat dirty tidak langsung buang, tampil konfirmasi inline di sticky footer (state `confirmDiscard`, tombol `.approval-btn--cancel`/`.approval-btn--danger`, TANPA `window.confirm`). Body scroll dikunci (`document.body.style.overflow`) selama sheet terbuka, 1 area scroll saja (Riwayat tidak punya `overflowY` sendiri). `saveStock` TIDAK disentuh (verified `git diff | grep saveStock/isTL/fieldsChanged` nihil).
  - **REVISI 5 SELESAI (3 keluhan: switch, data tidak sinkron, detail kurang elegan).** (a) Toggle Per Lokasi/Per Katalog jadi SAKLAR (`DataStokTab.jsx`, class `.stock-view-switch` di `src/styles/stock.css`, geometri menyalin `.theme-switch` header; seluruh baris teks+track satu `<button role="switch">` supaya area sentuh >=44px di HP). (b) **BUG DATA: status SAP di modal detail beda dengan daftar** — detail memakai `getSAPLabel(st.katalog)` (kode katalog saja), daftar memakai `stockSapLabel(st)` (hormati override `sapStatus` + aturan `STK-PREMEM-*`=Non-SAP). Resolver dipindah ke SATU tempat `src/lib/ragShared.mjs` (`resolveSapLabel`, `rowSapLabel`) karena `scripts/nightly_sync.mjs` (Node) tidak bisa mengimpor `sap.js`; `sap.js` sekarang delegasi ke situ, `buildKatalogRagContent` + `top20ByValue` (App.jsx & nightly_sync) ikut `rowSapLabel` — ini yang sebelumnya bikin status salah nyampai ke bot Telegram/web. (c) Footer detail jadi grid kolom sama lebar (label dipendekkan "Kartu Gantung"), tab Riwayat dirombak: strip ringkas Total masuk/keluar/Stok sekarang, tiap baris berlabel "Barang Masuk/Keluar" + jenis dokumen (field `docType` baru di event `buildKartuGantungHistory`) + no. bon, sisa di kanan; baris baseline "Migrasi Data" tidak lagi ditampilkan sebagai mutasi.
  - **DI-COMMIT & PUSH ke `main` (2026-08-11 malam).** Dua commit: rombak Data Stok (B1-B4 + Revisi 3-5) dan fix QR barcode. Padding footer sheet detail ditambah (tombol tidak mepet tepi) + di HP jadi 2 kolom, Hapus melebar sendiri.
  - **QR kartu gantung tidak lagi menunjuk localhost.** `window.location.origin` dipakai di 4 tempat (`KartuGantungModal.jsx`, `StockOpnameTab.jsx`, `docBuilders.js` x2) — kartu yang dicetak dari dev server menyimpan URL localhost/IP LAN yang mati di luar jaringan. Format URL scan dipusatkan di `scanUrlFor()` (`src/lib/utils.js`) + `PUBLIC_APP_ORIGIN`: pakai `VITE_PUBLIC_APP_URL` kalau diisi, kalau tidak `https://warnoto.vercel.app` saat origin terdeteksi lokal (localhost/127./::1/10./192.168./172.16-31.), selain itu origin apa adanya (production tidak berubah perilaku). **Kartu yang TERLANJUR dicetak dari localhost harus dicetak ulang** — QR lama tidak bisa diperbaiki dari kode.

- **Sesi 2026-08-11 (Opus, siang) — fix freeze buka menu ATTB + override status SAP/Non-SAP saat import. SEMUA DI-PUSH (`fae1db6`→`03b0522`→`06e3edf`), build hijau.**
  1. **FIX FREEZE ATTB (`06e3edf`, root cause):** `subGudangKodeMap` (`src/lib/masterSync.js`) infinite `while` loop. Suffix anti-tabrakan `(base.slice(0,2)+n).slice(0,3)` **jenuh** jadi "XY1" untuk n≥10 (10→"XY10"→potong→"XY1", 11→"XY1", …) → kalau "XY1" sudah dipakai, loop tak pernah keluar → **hang main-thread**. Data nyata: ~48 Sub Gudang semua "GUDANG TERBUKA/TERTUTUP …" → `subGudangAbbr` = "TRB"/"TRT" (2 huruf awal semua "TR") → kolam suffix "TR#" habis → freeze saat AttbTab render (`subGudangKodeMap(subGudangList)` dgn **list PENUH semua gudang**; caller lain aman krn subset per-gudang `filter gudangId`). **Fix 1 baris:** suffix injektif thd n (buang `.slice(0,3)`) → kode selalu unik, loop dijamin berhenti. Verified: node assert 48 nama patologis → 48 kode unik tanpa hang + live browser buka ATTB render penuh (21 baris, pipeline 6). **Gotcha investigasi:** hipotesis awal (realtime `stocks` setState-in-render) SALAH — cuma bystander; RBAC guard (`App.jsx:1679`, useEffect) mem-bounce ATTB ke Dashboard saat rolePerms belum load (bukan bug, hilang setelah load) → menutupi repro. `.click()` sidebar via DOM tak reliable switch tab; pakai panggil React `onClick` prop langsung + tunggu rolePerms.
  2. **Override Status SAP/Non-SAP eksplisit saat Import (`fae1db6`+`03b0522`):** classifier `getSAPLabel`/`getSAPStatus` kini strip leading-zero sebelum test panjang digit (kode padded `000000004180001` = 15 digit dulu keliru Non-SAP; angka asli 7 digit = SAP Persediaan). Helper baru `resolveSapLabel`/`katalogSapLabel`/`katalogSapStatus` (`sap.js`) + field `sapStatus` disimpan di katalog/stock saat Import Template WARNOTO (kolom "Status Material" di template + radio "Verifikasi Status Material" AUTO/SAP/Non-SAP di preview) — dihormati di DataStok/MasterData/Barcode/Opname/docBuilders/supabaseSync + Dashboard Analitik (`analytics.js` salin `kat.sapStatus`). `TPL_JENIS_ENUM` +Non-Stock.
  3. **Filter per-UPT menu ATTB:** opsi filter UPT (`AttbTab.jsx`) kini dari **cakupan viewer** (`getScopeUptIds`), bukan cuma UPT yang sudah punya data — global/UIT bisa pilih UPT mana pun (termasuk yang kosong). Sebelumnya dropdown sembunyi krn `uptOptions` dari `attbList` (cuma Surabaya). Data ATTB di DB memang baru Surabaya (150); UPT lain kosong sampai diisi. Terverifikasi: dropdown 6 UPT, pilih Gresik→0 item, Surabaya→150.
  - **Config (bukan repo):** plugin `impeccable@impeccable` DIMATIKAN (`~/.claude/settings.json`, boros token hook Stop); pakai skill desain hanya saat dipanggil user (frontend-design/design-taste-frontend/redesign-existing-projects/web-design-guidelines).

- **Sesi 2026-08-11 (Opus) — input 21 material non-stock Surabaya + fitur Status Material SAP. Kode DI-PUSH, build hijau; data self-host committed.**
  1. **21 material non-stock UPT Surabaya** masuk self-host (`stocks` id `STK-PREMEM-*` + 16 `katalog` baru; 4 kode sudah ada di-reuse). Nama+kode = **katalog MARA** (dicocokkan `migration-tools/match_nonstock_mara.py`, di-review/override manual, ditandai hijau di `D:\CLAUDE\WARNOTO data\Data Material Gudang\Non Stock\Non Stock.xlsx` sheet "Data Barang Baru"). `jenisBarang="Pre Memory"`, lokasi **Ketintang → GUDANG TERTUTUP KETINTANG** (Rak A–D = LOK existing), foto **sengaja kosong** (tim lapangan isi). Insert atomic (dry-run ROLLBACK → COMMIT).
  2. **Fitur Status Material SAP di Data Stok** (semua UPT/material): `getSAPLabel` (3-nilai: `SAP — Persediaan`/`SAP — Cadang`/`Non-SAP`) + badge 3-warna `sapBadgeStyleForLabel` + helper `stockSapLabel` (**hanya `id STK-PREMEM-*` = Non-SAP**; material lain ikut format kode katalog — 10-digit=Cadang, 7-8=Persediaan). Kolom "Lokasi UPT" kini derive dari **gudang** (`gdg?.uptId`, bukan `st.uptId` yg kosong). **Filter Status Material** baru (`STATUS_SAP` di `constants.js`). File: `sap.js`, `constants.js`, `DataStokTab.jsx`, `App.jsx`.
  - **Gotcha**: status SAP display-only (derivasi, bukan kolom DB) — awalnya aturan id-origin `bukan STK-SAP-*` kelewat lebar bikin STK-MIG-* SAP-Cadang keliru jadi Non-SAP; dipersempit ke `STK-PREMEM-*` saja. Kode MARA = tabel material master SAP, jadi 21 material punya kode format-SAP tapi status Non-SAP (kandidat masuk SAP, belum terdaftar).
  - **SISA besok:** tim lapangan upload foto 21 material (Data Stok → Ketintang → GUDANG TERTUTUP KETINTANG); verifikasi browser Data Stok (kolom Status: hanya 21 Non-SAP, sisanya seperti semula; Lokasi UPT terisi; filter Status jalan). Satuan 21 material masih kosong (sheet belum isi) — bisa dilengkapi admin saat isi foto.

- **Sesi 2026-08-10/11 (Opus, onboarding 5 UPT + polish UI) — kode `31bfbf0`→`4f71895` DI-PUSH + Edge Function DEPLOY, build hijau.** Data self-host langsung dari Google Sheet UIT JBM (`docs.google.com/.../gid=361941646`):
  1. **Master gudang** 15 baru (Malang/Madiun/Probolinggo/Bali/Gresik, `upt_id` FK; id `GDG-CAP-<SHEETUPT>-<GUDANG>`) + koreksi koordinat Babat/Tanah Lot/Tandes.
  2. **sub_gudang + warehouse_capacity** 36+36 baris utk 5 UPT non-Surabaya (menu Kapasitas mereka kini terisi; status ≥90%KRITIS/≥75%WASPADA; `warehouse_capacity.upt` = string UPPERCASE sheet SURABAYA/MALANG/dst).
  3. **satpam** 38 di-assign gudang (field `data.gudangId`; Yudi Hartono dihapus).
  4. **tim_mutu** jadi 18 (6 UPT × 3 kategori: >300jt/≤300jt/**Non Teknik**; field `uptId`+`kategori`, nama Title Case; `TM-300PLUS`/`TM-300MIN` dipakai ulang utk Surabaya = kompat txn lama).
  5. **Akun 15 baru + 5 reset** — Gresik/Malang/Madiun/Probolinggo/Bali lengkap 1 ADMIN/TL/ASMAN/MANAGER, password `123456` (baru via `bulk_create_users.mjs` service_role; reset existing via bcrypt `crypt()` langsung DB).
  - **Kode DI-PUSH:** filter tim_mutu TUG-4 by `txn.uptId` (`TUG3Tab`); dropdown filter UPT di Master Data gudang/satpam/tim_mutu utk viewer lintas-UPT (`MasterDataTab` `masterUptFilter`); satpam wajib gudang; dropdown gudang Data Stok difilter `st.uptId` + kolom "Lokasi UPT" (viewer lintas-UPT); banner Forecast 3-view diseragamkan ([switch]→banner→body); Migrasi Data ditata (grup Import Stok vs Histori 2-kolom, stepper hanya saat wizard SAP). Lanjutan pasca-onboarding: "Alat Perbaikan Data Lanjutan" (Master Gudang) dibatasi ADMIN/SUPERADMIN (`d1fc45f`); Peta Wilayah Gudang di Dashboard discope per-UPT login + `fitBounds` nasional/UIT zoom-out (`f2e058b`); polish korporat (review impeccable) **Kapasitas Gudang** (de-emoji, kontras teks status aksesibel, tabular-nums, copy Indonesia) & **Dashboard Overview Gudang** (hilangkan double-banner → strip tanggal, angka KPI dominan 21/17px, anti-overflow rupiah HP) — banner & palet dipertahankan (`4f71895`).
  - **Edge Function baru `sync-kapasitas`** (`supabase/functions/sync-kapasitas/index.ts`, Deno): fetch Google Sheet server-side (lolos CORS) → upsert `gudang`/`sub_gudang` **create-if-missing** (jaga denah/edit) + `warehouse_capacity` **refresh**; gate ADMIN/SUPERADMIN via JWT, service_role hanya di server. **DEPLOY manual self-host** (`vps-dr-stack/volumes/functions/sync-kapasitas/` + `docker restart supabase-edge-functions`; tes **HTTP 401** non-admin). Tombol "🔄 Sinkron dari Sheet" di menu Kapasitas (ADMIN); tombol "Import Data Gudang (Excel)" di Master Gudang **DIHAPUS**.
  - **Keputusan mengikat:** penambahan gudang berikutnya = manual/web/import template lokasi **atau** tombol Sinkron dari Sheet (bukan insert DB manual lagi). **SISA verifikasi browser:** klik "🔄 Sinkron dari Sheet" sbg admin (end-to-end JWT+reload); dropdown tim mutu TUG-4 per-UPT; filter UPT master data; kolom Lokasi UPT.

- **Sesi 2026-08-10 (Opus) — SEMUA DI-PUSH ke `main` (`79ed7a7`→`76cb6a5`), build hijau tiap commit.** Deret perbaikan + 1 tabel baru:
  1. **MARA search per-kata** (`05f1612`): "Cari Referensi MARA" (Tambah Katalog + Stock Opname) tak lagi menuntut frasa kontigu — `maraQueryGroups`/`applyMaraNameSearch` di `sap.js` (AND antar kata, OR sinonim; `.or()` dirantai = AND-of-OR PostgREST). `expandQueryForIlikeSearch` dihapus. Bonus: guard `import.meta.env || {}` di `supabaseClient.js` supaya modul app bisa di-`node --test`.
  2. **Foto Data Stok** (`b1c686c`,`e3846a3`): `updateStockFoto` dulu klaim sukses walau `saveToCloud` gagal (sesi expired/RLS) → "upload berhasil tapi DB tak berubah"; kini hormati return, `pendingFoto` ditahan kalau gagal. Path Storage `uploadStockFoto` tak lagi hardcode `upt-surabaya/` → per-UPT via `uptId` (4 caller). `extraColsStocks` isi `upt_id` kosong dgn UPT penulis.
  3. **Material Cadang DURABLE per-UPT** (`f308cdc`): AKAR "Material Cadang hilang" = dulu localStorage-only (tabel per-row `material_cadang_*` tak pernah ada, `syncMaterialCadangRows` gagal senyap; `window.storage` cuma di Artifact). **Tabel baru `material_cadang_state(upt_id PK, data/health/ai jsonb)` + RLS `can_access_upt(upt_id)` SUDAH DI-APPLY prod.** `saveToCloud` upsert blob per-UPT (masuk `failedLabels`); load baca server (RLS-scope, merge) + fallback localStorage + seed sekali. Jalur mati `syncMaterialCadangRows` (12 call + fungsi) dihapus. **Data Material Cadang fajar (UPT-SBY) SUDAH DIPULIHKAN** ke server dari localStorage localhost (2 analyses, 113 healthResults, 5 AI insights) via sesi fajar.
  4. **Alat Berat**: banner overdue dipindah ke channel "Peminjaman & Histori" (dulu bocor di atas Daftar Alat) (`3b9a80a`); super admin/Pusat bisa pilih UPT peminjam (`canChooseRequesterUpt`, dulu `requesterUpt=""` blokir submit) (`878b54e`); dropdown alat di-sort by UPT→nama (`0e58ac7`); **approval discope ke Asman UPT PEMILIK alat** (`76cb6a5`, gate `ownerUpt` — dulu keliru `requesterUpt`; pesan/PDF/`requiredApproverUpt` sudah "pemilik").
  5. **Data Stok filter UPT** (`336acca`): stok tanpa gudang (mis. 14 material Gresik) tak muncul saat filter UPT — filter klien resolve UPT hanya via gudang; diperbaiki 3 titik (`uptOf`, `filteredStocks`, `scopedEnrichedStocks`) cermin RLS `COALESCE(gudang.upt_id, s.uptId)`.
  6. **Kelola Akun**: dropdown filter per-UPT untuk role lintas-UPT (GLOBAL/PUSAT/UIT), admin UPT tanpa filter (`8e4d6be`); field "Batasi Akses Gudang" dihapus dari form akun (akses melekat ke `upt_id`; logika `allowedGudangIds` dibiarkan) (`8628f06`).
  - **Gotcha infra**: alias `~/.ssh/config` `minipc-gudang` sempat basi (IP `.231`→`.202`), sudah diperbaiki + host key diterima ulang. Screenshot CDP mati sesi ini (canvas Leaflet) — verifikasi UI pakai `read_page`/`javascript_tool`, bukan screenshot.

- **Template Migrasi Stok per-UPT (data referensi UPT Gresik) — DI-COMMIT (2026-08-09).** Rantai 3 script generator (semua reuse lib `xlsx` terpasang, tak ada dependensi baru): `scripts/gen_template_migrasi_stok.mjs` bikin `TEMPLATE_MIGRASI_STOK.xlsx` kosong (2 sheet: Petunjuk + Data Stok, 15 kolom — **UPT** di depan, lalu No Katalog/Nama/Satuan/Jenis Barang/Merk/Type/Kategori/Qty/Harga/Min Qty/Gudang/Blok/Foto Nameplate/Foto Keseluruhan); `fill_template_gresik.mjs` isi 14 material dari laporan SAP `WARNOTO data\tester\upt gresik\UPT GRESIK MATERIAL.xlsx` (ZST1 NORMAL→Persediaan, ZCAD→Cadang); `enrich_template_from_db.mjs` tarik Blok (4) + Foto (4, dari `mutasiBarang."Link Foto Material/Barang"` Gresik-only, dikonversi ke direct-view `lh3.googleusercontent.com/d/<id>`) dari `WARNOTO data\tester\DB_Warnoto.xlsx`. **Guard UPT:** `scripts/validate_template_upt.mjs` cek 1-file-1-UPT → WARNING + nomor baris Excel kalau tercampur UPT lain (exit 3). Scoping stock canonical = field `upt_id`/`uptId` (`deriveStockUptId`, `src/lib/stockScope.js`). **Keterbatasan sumber:** Harga & Merk TIDAK ada di file manapun (dibiarkan kosong); Foto hanya 2/4 link Drive yang benar-benar public/render (sisanya butuh share atau URL public). File foto lokal Gresik (9) ada di `WARNOTO data\Appsheet\_extracted\data`. `scripts/upload_gresik_foto.mjs` = tool upload file lokal→bucket `material-photos` (public URL), **DIPARKIR** — user memilih foto masuk lewat URL di xlsx via menu Migrasi Data, BUKAN upload self-host langsung.

- **Importer Template Migrasi Stok + fix "data hilang" + redesign Migrasi Data — SELESAI & TERVERIFIKASI end-to-end (2026-08-09).** Kartu baru **"Import Data Stok (Template WARNOTO)"** di `MigrasiDataTab.jsx` (state `tpl*`, `handleTplFile`/`applyTplImport`/`downloadTplTemplate`): parse `TEMPLATE_MIGRASI_STOK.xlsx` → guard UPT (kosong/tercampur/asing/bukan-UPT-admin, sebut nomor baris Excel) → preview per-baris + KPI → apply merge non-destruktif katalog+stok (key `uptId|katalogId|lokasiId`, foto https via `keepRemoteStockPhoto`, lokasi dari Gudang+Blok, `null` kalau tak ketemu). **AKAR "data hilang" (root cause):** RLS write `stocks` dulu WAJIB `lokasi_id` valid→gudang→`can_access_upt`; baris import `lokasi_id=null` (UPT baru belum punya gudang/lokasi) DITOLAK diam-diam → hilang saat reload. **Fix (migration `20260809_stocks_upt_id_scoping.sql`, SUDAH DI-APPLY prod + verifikasi):** tambah kolom `public.stocks.upt_id text` + backfill 247 baris lama dari lokasi→gudang; RLS baru scope `can_access_upt(coalesce((lokasi→gudang.upt_id), stocks.upt_id))` — pakai lokasi kalau ada, else kolom `upt_id`. Kode: `extraColsStocks` (App.jsx:1270) kirim `upt_id`; `applyTplImport` set `uptId` per baris + `saveToCloud` 2-tahap berurutan dgn hint (`katalogChangedRows` dulu → `stocksChangedRows`, jaga FK & hindari full-sync 18MB). **Verifikasi DB:** `stocks` UPT-SBY 247 + **UPT-GRS 14** (qty sesuai template, `lokasi_id=null` diisi manual nanti, 4 baris foto Drive). Plus: **Matrix Izin → SUPERADMIN-only** (`AppSidebar.jsx:128` + `MasterDataTab.jsx:804` gate `role==="SUPERADMIN"`; `menu.perms` di perms.js ternyata dead-key utk sub-tab, gate nyata di `hasRole`); **tombol "↩️ Batalkan Migrasi Terakhir"** (`lastTplImport` → hapus katalog-baru+stok import terakhir); tombol upload MARA menggantung DIHAPUS (matching MARA tetap via DB `mara_catalog`); layout Migrasi Data jadi 3 section berlabel (Template WARNOTO utama · SAP PEMAT collapsible · TUG Lama histori). **Keputusan mengikat:** scoping `stocks` = via `lokasi_id→gudang.upt_id` KALAU ada lokasi, else kolom `stocks.upt_id`; stok BOLEH `lokasi_id=null` (gudang diisi admin manual belakangan). **SISA:** verifikasi browser tombol Batalkan + ADMIN tak lihat Matrix Izin.

- **Cockpit Rekomendasi Pengadaan + scoping Material Cadang per-UPT — BELUM COMMIT saat ini (2026-08-09, sesudah gelombang besar).** (1) **Fix leak scoping Material Cadang**: analisis MC tak ter-scope UPT (record tak simpan `uptId`, `latestAnalysis`/`latestHealthRun` ambil `.slice(-1)` global → UPT lain lihat punya UPT lain). Fix: stamp `uptId: currentUser?.uptId` di record baru (`newAnalysis`/`importRecord`/`healthRun`/entry apply) + helper `inMcScope` (broadScope UIT/Pusat lihat semua; UPT lihat uptId sendiri). (2) **Fallback baca-waktu (TANPA tulis data)**: record LAMA tanpa uptId dinilai dari UPT pembuat (`recUptId`: `createdBy`/`requestedBy`/`importedBy` → `users.uptId`) di `MaterialCadangTab` (`inMcScope`) & cockpit `ForecastStokPage` (`runUptId`) — supaya data lama muncul lagi untuk UPT pembuatnya, tetap tak bocor lintas-UPT. (3) **Menu Forecast Stok jadi cockpit** (via skill impeccable init→shape→build, `PRODUCT.md` dibuat): tab diurut `Forecast Stok · Material Cadang · Rekomendasi Pengadaan`; view Rekomendasi Pengadaan = cockpit read-only 3 pita — header KPI agregat gabungan (spare health + procurement) · 2 kolom (kiri kesehatan Material Cadang [dashboard MC dipindah ke sini, subtab "Dashboard" DICABUT dari `MaterialCadangTab`], kanan tabel usulan beli `computeProcurementList`) · strip banding per-UPT (drill-down lensa). **Lensa UPT/UIT** dari scope permission (`uptScopeOptions=stockUptFilterOptions`, tampil bila ≥2 UPT), bukan UPT-berstok. Banner "Proyeksi Persediaan" dibatasi `forecastView==="forecast"` (tak double di cockpit). Tanpa tombol tulis di cockpit (aksi apply tetap di menu Material Cadang; empty-state kiri = link navigasi netral). **Craft-floor impeccable**: 0 error detektor. **SISA:** verifikasi browser (screenshot CDP mati sesi ini — user cek manual): Fajar/UPT lihat Material Cadang terisi, UIT lihat lensa + strip banding, cockpit 1 banner.

- **Material Cadang — GELOMBANG BESAR (2026-08-09), commit `5cd9ad7`→`d4ca97d`→`953d47e`, DI-PUSH.** (1) **325 katalog peralatan** (CT/CB/PT/DS/LA/TRAFO) dari Rangkuman Gangguan HAR **didaftarkan ke Master Katalog production** via SQL (`katalog` 427→752, `ON CONFLICT (id) DO NOTHING`; file `apply_registrasi_katalog_2026-08-09.sql` di `WARNOTO data\Data Material HAR`). Akar: material gangguan = peralatan terpasang, hampir semua belum ada di katalog gudang (cuma 3/993 MATCH). (2) **Importer pisah per Merk** (dedup key `noKat||cluster||merk`, `materialCadang.js`) + kolom Merk di tabel + guard peringatan UPT saat upload lintas-UPT (non-blocking). (3) **Fase 1 UI** (`MaterialCadangTab.jsx`): 6→5 tab (Health Index digabung ke "Analisis"), pagination, filter status, Apply/Setujui Semua batch. (4) **Fase 2 alur**: Apply approve → **buat/upsert baris Data Stok** gudang default UPT (qty 0, minQty=recommended **NON-DESTRUKTIF `max`**) via `ensureStockRow` (basis `allStocks` PENUH bukan scoped — jaga stok UPT lain); Rekomendasi Pengadaan diangkat ke `analytics.js` (`computeProcurementList`/`computeKatalogRisk`) + kartu di `DashboardDefault`; **AI Insight 4-aspek** (ringkasan eksekutif/diagnosis-per-material/prioritas-pengadaan/metodologi) + fallback lokal. (5) **Asman bisa apply LANGSUNG** (`handleApplyDirect`, tanpa pending); material sudah apply → tombol abu-abu (baris tetap tampil); history apply di tab Approval. (6) Fix blank page Rekomendasi Pengadaan (`953d47e`): `DEFAULT_LEAD_TIME_DAYS`/`MIN_HISTORY_MONTHS` di-EXPORT dari `analytics.js` (refactor Fase-2 tinggalkan referensi di teks metodologi ForecastStokPage → crash render semua role). **Deliverable data (di `WARNOTO data\Data Material HAR`, BUKAN repo):** rekap gangguan per-UPT + 6 file `IMPORT_MaterialCadang_UPT-*` (No Katalog dari kolom `No. Material` sheet per-UPT; Populasi per-voltage STATUS Operasi dari `Populasi Aset UIT JBM.xlsx`; TTF dari Tahun Operasi lama; LeadTime 180; hanya material has-history). **Keputusan mengikat:** Merk = FLAG (1 nomor katalog = 1 entri, merk disimpan sebagai daftar), BUKAN katalog terpisah. **SISA verifikasi browser end-to-end:** apply→baris stok muncul di Data Stok, kartu Dashboard, AI dgn Groq live, parity nilai procurement pra/pasca-refactor.

- **Fix Nilai Inventory Dashboard "Rp 0" — DI-PUSH (`2b80af9`, 2026-08-08, verified).** `totalVal` (`App.jsx:5078`) = `Σ s.qty*s.price` TANPA guard → ≥1 baris stok dgn `qty`/`price` undefined/string-non-numerik hasilkan `NaN` yang meracuni SELURUH reduce → `NaN` → `fmtRp(NaN)` tampil "Rp 0". Scope/RLS/data SEMUA benar (dibuktikan via log debug sementara: `dataScope=['UPT-SBY']`, `scoped=246/246`, `unscopedVal=7.554.022.774`). Fix: `a+(Number(s.qty)*Number(s.price)||0)` netralkan per-baris. Pelajaran: `buildStockStats` sudah guard `(s.qty||0)*(s.price||0)`; hanya `totalVal` yang bolong. Sisa (opsional, DATA): baris stok berharga rusak yg picu NaN — kini dihitung 0, aman; betulkan harga = kerja data terpisah.

- **Fix akurasi Dashboard "SAP vs Fisik" — DI-PUSH (`b0426dd`, 2026-08-08).** Dashboard baca `stockCountList[0].summary.akuratPct` (sesi terbaru) tapi list tak di-sort saat load → urutan acak Supabase; kolom `created_at` kembar (tiap `syncMasterTable` timpa `Date.now()` ke semua baris) → tie-break acak nyangkut ke sesi lama 100% bukan terbaru 95%. Fix 3 lapis: (1) `App.jsx` sort `stockCountList` by `uploadedAt` di load (scLocal L861 + cscRemote L878, helper `byRecency`); (2) `masterSync.js` `created_at` stock_count derive dari `item.uploadedAt` (2 fungsi sync) → stop tie ke depan; (3) data DB 2 baris `stock_count.created_at` dikoreksi = `uploadedAt` (UPDATE manual, sudah jalan). Recency stock_count = `uploadedAt` (BUKAN kolom `created_at`). stock_opname pakai `dibuatAt` (di luar scope, 1 baris tanpa tie).

- **Filter per-UPT UI multi-UPT — DILENGKAPI & DI-PUSH (`e3ff9d1`, 2026-08-08).** Viewer UIT/Pusat kini punya dropdown filter per-UPT (pola `stockUptFilter` Data Stok, muncul hanya viewer multi-UPT) di TUG (`tugUptFilter`→`filteredTxns`+sub-tab TUG3/5/15), Alat Berat & ATTB (perluas `effectiveUptFilter` via `roleTier`), BA Inspeksi (`baUptFilter`). Banner Maturity: kotak Level sudah per-UPT (benar); switcher UPT dipaksa ke baris sendiri (`width:100%`) supaya Level tak geser. Maturity `maturity_assessments` memang NASIONAL (tak ada `upt_id`) — audit/5S/evidence-Drive semua per-UPT (dikonfirmasi). Opsi filter dari SCOPE viewer (`getScopeUptIds`), bukan data yang ada (UPT tanpa data tetap bisa dipilih).

- **Kapasitas → Peta Utilisasi — DI-COMMIT & DI-PUSH (`23ed584`, 2026-08-08).** Filter UPT peta (`petaUptFilter` by `gudang.uptId`, opsi dari `getScopeUptIds`, oper `uptList` ke KapasitasGudangTab). Fix stale: PetaGudangTab useEffect kosongkan `selectedGudangId` saat `gudangList` kosong (UPT tanpa gudang tak nyangkut sub-gudang UPT lain). Redesign toolbar: satukan selector jadi 1 panel berlabel drill-down (1.UPT → 2.Gudang → 3.Denah) + checkbox, hapus `capacity-filterbar` orphan. **Verifikasi browser OK (2026-08-08).**

- **Cari Foto (Data Stok) filter UPT — DIPERBAIKI & DI-PUSH (`f270f38`, 2026-08-08).** `runPhotoSearch` kini batasi hasil ke katalog yang punya stok dalam scope efektif / `stockUptFilter`. Dulu dua mode (nameplate & bentuk) abaikan scope. **Verifikasi browser OK (2026-08-08).**

- **Refactor App.jsx ke custom hooks — target user ≤4000 baris. Tranche-1..6.** Tranche-1 Maturity→`useMaturity.jsx`, Tranche-2 OCR/denah→`useDenahOcr.js`, Tranche-3 Alat Berat→`useHeavyEquipment.js` (`d89dee6`, verifikasi browser OK, DI-PUSH). **Tranche-4/5/6 (2026-08-09, BELUM PUSH, commit lokal `7f130ed`/`71ba08a`/`a451054`):** Tranche-4 Manajemen Akun→`useAccountAdmin.js`, Tranche-5 CRUD master-data entitas (Katalog/Satpam/TimMutu/UIT/UPT/ULTG)→`useMasterDataCrud.jsx`, Tranche-6 Konfigurasi Gudang (Gudang/Lokasi/subGudang/denah-tools/Capacity)→`useWarehouseConfig.jsx` (dep sirkuler dgn useDenahOcr via urutan+stateRef). **Tranche-7..10 (2026-08-10, BELUM PUSH, commit lokal `e98ff65`/`60d1026`/`63a81a7`/`afe2d16`):** Tranche-7 mesin approval TUG (20 handler approveTUG*/rejectTUG*/submit*)→`useTugApprovals.js`, Tranche-8 form/commit transaksi TUG→`useTugTransactions.js` (stateRef utk submitDraftTug9/enrichedStocks), Tranche-9 Stock Opname & Count→`useStockOpname.js`, Tranche-10 approval-hub Lokasi + pagination approval→`useApprovalHub.js`. **App.jsx 5923→4000 (−1923, −32%), TARGET ≤4000 TERCAPAI.** Tranche-7..10 diverifikasi POST-LOGIN di browser (sesi Fajar Sutomo/UPT-SBY live): tab Approval render penuh + form "Terima Barang Baru" buka + menu Stock Opname render data nyata, console bersih tiap kali. 10 custom hooks total di `src/hooks/`. `logApprovalHistory`/`approveTxn`/`rejectTxn` generik & `renderApprovalPager` sengaja tetap di App.jsx (lintas-hook/butuh sty). Temuan: `mapConfigMode` dead code (Tranche-6, belum dihapus). **Tranche-4/5/6 (Akun/Master-Data/Gudang) sudah verifikasi post-login juga (2026-08-09 user fresh-login = app normal).** **ATURAN MUTLAK TDZ (dari bug tranche-1):** sebelum taruh `const {...} = useX()`, grep semua identifier domain di App.jsx, pastikan destructure ADA DI ATAS pemakaian render-body pertama (assignment `stateRef.current`, dep-array useEffect/useMemo, JSX); pemakaian di closure/handler aman di mana saja. Tranche-3 pakai pola `stateRef.current.saveToCloud` (hook dipanggil sebelum `saveToCloud` didefinisikan — diisi belakangan lewat mutasi stateRef). **Gotcha alat berat (bukan refactor):** dropdown "Alat di luar UPT" kosong krn RLS `heavy_equipment` scoped baca ke UPT sendiri; DIPERBAIKI (`d89dee6`, migration `20260808_heavy_equipment_read_lintas_upt.sql` applied): SELECT dibuka authenticated (inventaris lintas-UPT utk pinjam), write tetap scoped `can_access_upt_nama`.

- **Multi-UPT 3-tier SELESAI & diverifikasi user di browser, DI-PUSH ke `main` (2026-08-07).** Scoping UI + label tier (nasional→"PLN Pusat", UIT→kode "UIT JBM", UPT→nama UPT) + dokumen per-UPT (`docBuilders.js`: kop/PIC/ttd/tempat pakai NAMA UPT, nomor pakai KODE UPT). Data display discope (stok via `gudang.uptId`, TUG `scopedTxns`). Fix 403 seed gudang/lokasi (`aad4704`, seed DEFAULT hanya viewer nasional). RAG per-UPT: migration `20260807_rag_chunks_per_upt.sql` **DI-APPLY** (kolom `rag_chunks.upt_id`; RPC `match_rag_chunks(query_embedding, match_count, p_upts text[] default null)`, null=nasional). Tombol Sync KB kini utk ADMIN_LOG_PUSAT & role UIT. Filter UPT di Data Stok utk viewer multi-UPT (`c49c937`). Master `upt` lengkap 6 UPT (SBY/MLG/MDN/PBG/BLI/GRS, semua UIT-JBM). **SISA:** begitu UPT baru (Gresik dll) diisi gudang→lokasi→stok oleh adminnya, jalankan Sync KB dari akun nasional/UIT utk generate chunk RAG-nya. Bot Telegram masih 2-arg=nasional (fase 2: butuh `upt_id` di `tg_allowed_users`).

- **Gelombang 4c DI-APPLY ke production & TERVERIFIKASI (2026-08-08).** verify_gelombang4c.sql LULUS: GRANT tulis anon=0, SELECT publik tepat 3 (katalog/stock_current/tug15_history utk scan QR), tulis anon GAGAL (`permission denied warnoto_state`), storage INSERT/UPDATE publik=0, baca anon warnoto_state/forecast/scan_log=0. Code `8f455e1` (2026-08-06).** Menutup jalur tulis/baca `anon`. Akar masalah DI KODE: 3 fungsi sync (`syncTUG15ToSupabase`, `syncStockQtyToSupabase`, `syncFotoMaterialToSupabase` di `src/lib/supabaseSync.js`) mengirim anon key sbg `Authorization`, bukan token sesi — diperbaiki helper `authHeaders()` (apikey=anon, Authorization=session token, tanpa sesi melempar Error). Isi migration `20260806_tutup_tulis_anon_gelombang4c.sql`: cabut GRANT tulis anon seluruh public + `alter default privileges`; tulis `katalog`/`warnoto_state`→authenticated; hapus policy tulis publik `wa_sync_status`/`tg_agent_logs` (penulisnya Edge Function service_role, aman); baca `warnoto_state`/`forecast_predictions`/`stock_scan_log`→authenticated; storage buckets `material-photos`/`stock-photos`/`tug-photos` INSERT+UPDATE→authenticated. **SENGAJA tetap terbuka anon (jangan "dirapikan"):** baca `katalog`/`tug15_history`/`stock_current` + SELECT 3 bucket foto (dipakai scan QR publik `ScanPublicView.jsx`). **Temuan terbesar:** `warnoto_state.state_data` (blob seluruh state app) bisa dibaca tanpa login sebelum 4c.

- **Gelombang 4a DI-APPLY ke production & TERVERIFIKASI (2026-08-08).** `20260805_multi_upt_rls_gelombang4a.sql` (`d6f657e`). verify_gelombang4a.sql LULUS: sisa policy lama=0, scan QR hidup, VIEWER UPT-SBY 3/4/38/2, **ADMIN_LOG_PUSAT & ASMAN_LOG_UIT tug_transactions=3** (dulu 0 — bug buta total sembuh). Menutup `stock_current`/`tug15_history` tulis anon→authenticated (baca publik dipertahankan utk scan QR); fix BUG NYATA `tug_transactions` (UIT & ADMIN_LOG_PUSAT dulu buta total, 0 dari 3 txn) via `can_access_upt()`; jangkar UPT `tug_items`/`tug_approvals`/`stock_movements`. `tug_review_tokens` SENGAJA tak disentuh (policy `actor_id=auth.uid()` sudah lebih ketat).

- **Audit GRANT `anon` (2026-08-06, read-only):** role `anon` pegang GRANT tulis pada **33 tabel** public (yang menahan hanya RLS). Yang benar-benar bisa ditulis TANPA LOGIN hari ini: `katalog`, `stock_current`, `tug15_history`, `wa_sync_status`, `warnoto_state`, `tg_agent_logs`. 4a menutup `stock_current`+`tug15_history`, 4c menutup sisanya. Pengetatan GRANT (paling serius `katalog`) = gelombang terpisah, cek dulu apakah bot menulis via anon atau service_role sebelum ketatkan.

- **Gelombang 4b SELESAI — ternyata skema sudah ter-apply (diverifikasi production 2026-08-08), TANPA DDL baru.** Audit menemukan kolom+RLS 4b sudah terpasang (kemungkinan ikut saat kerja multi-UPT 3-tier). `profiles`: RLS on, SELECT `can_read_profile(id,upt_id,uit_id)` (cabang `actor.id=p_profile_id` jamin self-read → login aman, 3 akun `upt_id` NULL tetap baca profil sendiri), TAK ada policy tulis → create/edit akun via Edge Function `admin-create-user`/`admin-update-user` (service_role, `App.jsx:1496`), anon SELECT only. `stock_opname`/`stock_count`: `upt_id text NOT NULL` terisi, scoped read+write via `can_access_upt` (USING+CHECK), anon nol GRANT tulis. Spot-check backfill LULUS: ketiga baris (1 opname + 2 count) `upt_id=UPT-SBY` cocok UPT uploader (`d7104036`=ADMIN Fajar Sutomo, UPT-SBY). Catatan desain (dibiarkan, bukan bug): VIEWER/UPT bisa baca profil rekan se-UPT (nama/role/HP) via `can_access_upt` — tak bocor lintas-UPT.

- **Halaman scan QR publik dirombak & PUSH (2026-08-11, `84b4f18`).** Lokasi kini terbit ke halaman publik lewat `lokasiPublik` di `katalog.data` (jsonb, TANPA DDL) yang ditulis `syncStockQtyToSupabase(stocks, katalogList, { lokasiList, subGudangList, gudangList })` — dibaca ulang lalu di-merge supaya `fotoKeseluruhanUrl` tak terhapus `resolution=merge-duplicates`. `TUG15Tab.jsx:76` sengaja memanggil tanpa master (tak ada `subGudangList` di scope) sehingga enrichment dilewati, bukan menulis lokasi kosong. `ScanPublicView.jsx`: kartu lokasi breadcrumb Gudang > Sub Gudang + blok besar (fallback perkiraan dari net keluar-masuk per `lokasi_kode` bila belum tersinkron), kartu asal barang dari mutasi MASUK terakhir, kalimat ringkasan bahasa awam, bar melayang sisa+blok, kartu "Apa arti halaman ini?". Terverifikasi di DB produksi: KAT-2230304 `lokasiPublik` = KETINTANG / GUDANG TERTUTUP KETINTANG / RAK-C qty 34. **Sisa: verifikasi user di `pln.warnoto.com` setelah deploy Vercel.**

## Langkah berikutnya (urut, mengikat)

**STOCK OPNAME — lanjutan (sesi berikutnya):**
1. **Verifikasi browser Fase 1 & 2** (belum dilakukan; di-push atas instruksi user). `npm run dev` :3001
   (GOTCHA: dev server sesi lalu berkali ke-kill watchdog — user jalankan `! npm run dev` sendiri). **Fase 1:**
   upload PID CSV/XLSX → dialog pilih gudang; zero-padding beda cocok; chip BLOK(qty)+filter; Lembar Hitung A4;
   anti-timpa 2-tab. **Fase 2** (device 360px): "📱 Mode Lapangan" → pilih blok/scan QR blok; **uji kritis:
   kursor di kolom qty lalu tembak scanner → angka TIDAK boleh kotor** (bukti `blockInput`+`onScanStart→blur`);
   scan 3 barang beruntun (`continuous`); iPhone/Safari fallback zxing (paksa `BarcodeDetector=undefined` di
   DevTools kalau tak ada perangkat iOS); reload → hitungan tetap (autosave); recount item selisih blokir submit.
2. **Fase 3** — freeze peringatan (`useStockOpname` `setOpnameFreeze` + `useTugTransactions` `saveTxn` warn).
   Detail di plan walrus Fase 3 (baris 157-171).

**Keputusan arsitektur (mengikat, dari Fase 1):** qty opname disimpan **per blok** (`hitungPerLokasi`), sesi
opname **dipecah per gudang**, simpan pakai **merge fetch-server** (`loadMasterTable("stock_opname")`, bukan cache
lokal) supaya tak timpa lintas-device. Recount wajib & freeze=peringatan menyusul Fase 2/3. Dependensi baru
`@zxing/browser` (Fase 2, iOS-only) disetujui.

**API INTEGRASI Fase 2 & 3 — PENDING JANGKA PANJANG (keputusan user 2026-08-18).** Fase 1 + 1.5 sudah live+tested
(lihat Status). Fase 2 & 3 SENGAJA ditunda tak berbatas waktu — TIDAK dibangun spekulatif, tunggu prasyarat eksternal.
- **Fase 2 — Ingest SAP→WARNOTO (master material sync):** endpoint `POST /master` (scope `write:master`), review-first
  (masuk staging/pending, tak langsung timpa master). BLOCKER: kontrak field-mapping dari tim SAP (nomor katalog,
  plant→UPT pakai `SAP_PLANT_TO_UPT` di `src/data/masterUpt.js`). Plan Fase 2 di
  `.claude/plans/task-notification-task-id-boyybm8kh-tas-crispy-tarjan.md`.
- **Fase 3 — WARNOTO→SAP (goods movement/TUG) + fallback file/batch:** `GET /tug?status=pending` (SAP poll dok TUG
  utk dibukukan) + export snapshot CSV/JSON. Push langsung ke SAP OData DITUNDA — butuh endpoint+kredensial OData SAP.
  BLOCKER: tim SAP sediakan target contract/creds + IT ekspos `warnoto.com` ke jaringan SAP.
- **Aktifkan lagi HANYA kalau:** tim SAP kasih kontrak/kredensial ATAU user minta eksplisit. Jangan mulai dari
  notifikasi/kuota; ini keputusan user.

**Varian gaya Taste Skill — DITUNDA (keputusan user 2026-08-15).** `minimalist-skill` SUDAH dipasang (2026-08-15) sebagai `minimalist-ui` scope user, dengan pagar presedensi di `docs/DESIGN_GUIDELINES.md` seksi 16 (guideline WARNOTO menang atas skill). `brutalist`/`soft`/`stitch` sengaja BELUM dipasang; nanti dipilih/disesuaikan dulu dengan `docs/DESIGN_GUIDELINES.md` supaya tidak ada dua sumber gaya yang bertabrakan. Dari repo itu yang sudah terpasang scope user: `taste-skill` (nama lokal `design-taste-frontend`), `redesign-skill` (`redesign-existing-projects`), `brandkit`, `image-to-code`.

**Verifikasi browser di rumah (sesi 2026-08-10, semua kode SUDAH di-push & deploy Vercel jalan):**
1. **Alat Berat — transaksi pinjam Probolinggo→Surabaya end-to-end** sebagai super admin ATAU alur nyata: Ajukan (owner=Probolinggo) → **approve oleh Philips Sihombing (Asman UPT-PBG, sudah ada)** atau super admin → Tandai Kembali → **Cetak dokumen (PDF)**. Pastikan nama Asman di PDF = Philips (label "Pemilik Alat" kini cocok). Catatan: Gresik & Probolinggo kini punya Asman (Philips) — approval tak lagi wajib super admin untuk PBG.
2. **Material Cadang** (Forecast Stok): setelah deploy, login fajar/UPT-SBY → data pulih tampil (server-durable). UPT lain lihat punyanya sendiri (isolasi RLS `material_cadang_state` sudah diuji: fajar diblok tulis UPT-GRS).
3. **Data Stok** filter UPT Gresik (super admin) → 14 material Gresik tanpa lokasi harus muncul.
4. **Kelola Akun**: dropdown filter UPT muncul untuk superadmin/Pusat/UIT (user bilang sudah cek visual OK); form akun tak ada lagi "Batasi Akses Gudang".

**Lanjutan multi-UPT UI — SELESAI & verifikasi browser OK (`23ed584`, `f270f38`, 2026-08-08):**
1. ✅ Peta Utilisasi filter UPT — committed + verified.
2. ✅ Cari Foto Data Stok filter UPT — fixed di `runPhotoSearch` (`allowedKatalog`) + verified.

**Rantai apply 4c→4a — SELESAI & TERVERIFIKASI di production (2026-08-08).** 4c lalu 4a di-apply via `ssh minipc-gudang-home` (`--single-transaction -v ON_ERROR_STOP=1`), kedua verify script LULUS, sisa policy lama=0. Verifikasi browser production akun nyata OK (2026-08-08) — simpan Data Stok & TUG-15 tetap tersimpan, UIT/ADMIN_LOG_PUSAT bisa lihat TUG.

**Gelombang 4b SELESAI (2026-08-08)** — skema ternyata sudah ter-apply, spot-check backfill lulus, tanpa DDL baru (detail di Status atas). Rantai RLS multi-UPT 4a→4b→4c TUNTAS.

**Pengetatan GRANT anon — SELESAI (2026-08-14, migration `20260814_cabut_baca_anon_vestigial.sql`).** Audit: tulis anon (INSERT/UPDATE/DELETE) sudah tertutup TOTAL di seluruh public (4a/4c beres, nol write grant tersisa). Yang dicabut sekarang cuma GRANT SELECT vestigial anon di `warnoto_state`/`wa_sync_status`/`tg_agent_logs` (RLS sudah blokir → nol dampak fungsional, defense-in-depth). `katalog` (+ stock_current/tug15_history) SENGAJA dibiarkan anon-SELECT = scan QR publik. Bot nulis via `SERVICE_ROLE` (dikonfirmasi env box), app authenticated — anon tak pernah jalur sah. Verified: anon SELECT ketiga tabel kosong, katalog utuh.

**Scan QR publik — DITUTUP (keputusan user 2026-08-08):** baca material lintas-UPT via scan = BUKAN risiko; yang penting scan tak bisa EDIT stok lintas-UPT — sudah dijamin gelombang 4a/4c (anon tak bisa tulis). Tak ada kerja.

**Bot Telegram fase 2 — DI-COMMIT & DI-PUSH (`6f780a2`, 2026-08-08). UPDATE 2026-08-14: SUDAH DEPLOY di self-host sejak Aug 9 (diverifikasi via diff box↔repo); GAP state nasional ditutup (lihat entri Status teratas, tinggal install file staged + restart edge). SISA cuma verifikasi browser dropdown scope form Kelola User Bot.** DB: `tg_allowed_users.upt_id` ditambah+backfill (id1=NULL global, id6/7/8=UPT-SBY; migration `20260808_tg_allowed_users_per_upt.sql` applied). Kode: `telegram-webhook/index.ts` scope RAG via `p_upts` (NULL=global lihat semua, else `[upt]` +chunk nasional; RPC tak berubah); form `TelegramWhitelistPanel.jsx` + `AIAgentPage.jsx` dropdown scope wajib (Global/per-UPT) di pendaftaran user baru. **SISA (utk Vendor C):** (a) verifikasi browser dropdown scope form Kelola User Bot; (b) **deploy manual** `telegram-webhook` ke self-host `minipc-gudang` (gated — `supabase functions deploy telegram-webhook`, cek gotcha deploy Edge Function self-host di memory). **GAP BELUM DIPUTUSKAN (ditandai `ponytail:` di index.ts L360):** `buildWarnotoStateContext` masih kirim ringkasan stok NASIONAL ke user Telegram scoped (blob `buildWarnotoStateSnapshot` App.jsx ~L4510 tanpa `upt_id` per item → filter murah mustahil, perlu restrukturisasi lintas-file). Rekomendasi arsitek: TERIMA dulu + debt. Tunggu keputusan user.

**Verifikasi user (browser, belum lunas):**
- Uji jenjang Maturity end-to-end akun sungguhan: TL buat audit → "Kirim Hasil ke UIT" (status `REVIEW_UIT`) → UIT review → Pusat/final. Cek negatif: MANAGER UPT tak boleh pindah UPT / simpan di jenjang atas.
- Buat 2 akun baru lewat Kelola Akun: `ASMAN_LOG_UIT` (pilih unit UIT) + `ADMIN_LOG_PUSAT` (nasional).
- Form 5S pasca-GELOMBANG B (`upt_id` NOT NULL): simpan pengisian nyata, refresh, cek muncul di History.
- Evidence Drive dengan akun Pusat/UIT (jalur nasional `maturity-drive` belum diuji end-to-end).
- Upload foto pasca-fix `ff7fd49` (Data Stok, Inspeksi Material).
- UI mobile Dashboard/Alat Berat/Data Stok pasca-deploy.

**Lain:**
- **Importer Template Migrasi Stok full-stock di menu Migrasi Data** (**JANGKA PANJANG / ditunda** per user 2026-08-14; belum dibangun): baca `TEMPLATE_MIGRASI_STOK.xlsx` (UPT+katalog+stok+lokasi+foto URL), map kolom UPT→`uptId`, ADOPSI guard `validate_template_upt.mjs` (tolak/WARNING file tercampur UPT), isi `fotoNameplate`/`fotoKeseluruhan` dari kolom Foto (URL). `MigrasiDataTab.jsx` sekarang hanya SAP PEMAT.
- ~~Blokir `warnoto.com` di kantor (DNS sinkhole FortiGuard)~~ — **CLEAR (2026-08-14, konfirmasi user)**: IT sudah whitelist domain, akses production dari kantor jalan. Cadangan proxy Vercel rewrite tidak jadi dipakai.
- Klarifikasi status `vps-dr-stack`: production dianggap tetap di sini atau pindah ke stack terpisah sesuai rencana DR awal?
- Migrasi Non-SAP UPT Surabaya review-first: 40 baris audit (34 kuat, 5 lemah, 1 tanpa kandidat) via UI Opname Non-SAP.
- i18n ditunda (tunggu arahan user).

**Blocker:** — (tidak ada; whitelist `warnoto.com` sudah CLEAR 2026-08-14).

## Perintah verifikasi
- `npm run dev` → port 3001 (akses via `localhost`)
- `npm run build`
- `node --test tests/unit/tugCanonical.contract.test.mjs tests/unit/telegramWebhook.contract.test.mjs`
- `node scripts/rehearse_tug_canonical_postgres.mjs`
- `npx playwright test tests/e2e/desktop.spec.js --project=desktop-smoke --grep "canonical derived TUG-8 draft"`
- `node --test tests/unit/heavyEquipmentPhoto.test.mjs tests/unit/stockLocationApproval.test.mjs`
- `npx playwright test tests/e2e/heavy-equipment.spec.js --project=desktop-smoke --workers=1`
- `npx playwright test tests/e2e/tug15-legacy.spec.js --project=desktop-smoke --project=tug15-mobile --workers=1`
- `node --test tests/unit/maturityDrive.security.contract.test.mjs`
- Deploy setelah persetujuan eksplisit user: `git push origin main`

## Riwayat shift (maksimal 2)
- 2026-08-26 Claude: **Review paralel per-item MATLEV Audit (`49c5db3`) + iterasi-2 (`8af9235`).** Tabel `maturity_aspect_reviews` (PK audit/aspect/item) + RLS UIT/Pusat lepas status → UIT Check/Reject per-item paralel dgn upload UPT; "Kirim ke Pusat" oleh UIT (gate semua item Checked); card compact+responsif HP. Iterasi-2: tombol Check state-aware + lock file UPT setelah Checked. Plus fix Kartu Gantung print (window.open sebelum await). 2 migration di-apply prod (gotcha grant authenticated). Verified browser. Pushed. Tabel `maturity_aspect_reviews` (PK audit/aspect/item) + RLS UIT/Pusat lepas status → UIT Check/Reject per-item bukti paralel dgn upload UPT; "Kirim ke Pusat" oleh UIT (gate semua item Checked); card evidence compact+responsif HP. 2 migration di-apply prod (gotcha: grant authenticated). Verified browser. Pushed.
- 2026-08-26 Claude: **Cetak BA Inspeksi Material Cadang dirombak (`9f093f6`+`4205de1`).** Fix pagination (portal ke body ganti position:absolute), landscape 2-halaman, foto hal.2 grid 3-kolom + preload, meta hal.1 (Tanggal/Gudang/No SLoc/UPT), TTD pelaksana terpisah, No SLoc map per-UPT. Verified via headless-Chrome PDF. User ACC. Pushed.
- 2026-09-02 Claude: **Fix print (`3cec3b1`) + cetak dokumen inti bersih A4 (`e750090`).** (1) CSP enforce blokir inline `onclick="window.print()"` di popup cetak (semua dokumen mati di prod, jalan di localhost) → `vercel.json` script-src `'unsafe-hashes' 'sha256-<window.print()>'`. (2) Dokumen inti (TUG-9/10/5/7/3 + BAST): unduh dulu simpan .html + print pakai atribut browser. Sempat coba jsPDF.html raster → user bilang berantakan (batas html2canvas layout padat). Pivot: buka HTML di tab (`window.open` sinkron, tombol Print sudah CSP-safe) + `@page margin:0` → print bersih tanpa header/footer browser, A4 dijamin, fidelitas engine browser asli. `pdfExport.js` dihapus. User ACC visual. PENDING (di luar cakupan): 13 dokumen sisa migrasi pola sama; 2 ide fitur lapangan (sumber kontrak material, marketplace ULTG).
- 2026-09-02 Claude (IMPLEMENTED item 1/5/3/4/A/B — build+unit test hijau (142 pass/6 fail = pre-existing identik HEAD, nol regresi); item 1/3/4 VERIFIED browser; item 2 WA BLOCKED provider; BELUM commit/push): **Rencana "5 Catatan Tambahan UIT-JBM + 2 fitur lapangan"** di `C:\Users\PLN\.claude\plans\ada-catatan-tambahan-dari-lazy-mochi.md` (disetujui user). Tanpa dependensi baru, semua reuse pola existing. **MIGRATION SUDAH DI-APPLY ke DB prod self-host 2026-09-02** (kedua file: `20260902_maturity_target.sql` kolom `target`, `20260902_notif_outbox.sql` 2 tabel+trigger). Trigger `trg_notif_outbox_on_tug_final` LIVE tapi AMAN (notif_recipients kosong → nol insert). **SUDAH DI-PUSH `d9e0105`** (Vercel auto-deploy). **E2E VERIFIED item 1/3/4/5** (login UIT-JBM). E2E menangkap 2 BUG (build+unit luput), sudah fix: (item 4) marker Alat Berat crash `Invalid LatLng` — offset `+0.006` ter-concat ke koordinat STRING dari `extractLatLngFromAddress`; fix `Number(repCoord.lat)+0.006` di App.jsx. (item 5) write target 403 — `maturity_audit_history` historisnya seed-only, DB self-host cuma punya READ policy (write policy schema.sql tak ke-migrasi cloud→self-host); fix tambah write policy di `20260902_maturity_target.sql`, sudah re-apply. **Item A/B CODE-verified, E2E BELUM** (butuh approve TUG-3 nyata = destruktif multi-role TL/Asman + dummy doc). Item 2: fondasi pushed; WA send + deploy EF `notify-dispatch` + pg_cron + wiring `NotifRecipientPanel` + provider WA masih nunggu. Item A/B E2E butuh transaksi TUG-3/reservasi nyata (mutasi data, belum diuji). Item 2 FONDASI SUDAH DIBANGUN (files-only, build hijau): `supabase/migrations/20260902_notif_outbox.sql` (tabel notif_outbox+notif_recipients+trigger FINAL_APPROVED TUG8/9 insert outbox, idempoten, tanpa network), `supabase/functions/notify-dispatch/index.ts` (skeleton: sendTelegram jalan salin pola telegram-webhook:522, sendWhatsApp STUB throw NOT_CONFIGURED), `src/components/NotifRecipientPanel.jsx` (CRUD penerima tiru TelegramWhitelistPanel, BELUM diwiring ke UI). SISA item 2: pilih provider WA → isi sendWhatsApp; apply migration; deploy EF; pg_cron pemanggil notify-dispatch; wiring panel ke Master Data. JANGAN bangun ulang fondasi.** Urutan: **(1)** BUG chip Data Stok "kritis"/"belum ada lokasi" tak sinkron filter UPT/Jenis (`App.jsx:3735-3761` angkat `mq`, hitung array-antara, prop ke `DataStokTab.jsx:87-88`). **(5)** Target Matlev per UPT/semester (`add column target` di `maturity_audit_history` yg sudah unique upt/tahun/semester + garis putus di `MaturityDashboardTab.jsx:369-385`). **(3)** Warna marker peta per UPT (`UPT_MAP_COLOR` di `theme.js`, `App.jsx:1631-1641`, legenda multi-UPT). **(4)** Overlay Alat Berat agregat per UPT di peta (butuh 3; `heavyEquipment.js`, `stripUptPrefix`). **(A)** Kontrak material LITE — bawa metadata kontrak TUG-3 (`useTugApprovals.js:97-148`) ke `stock.data.kontrakRefs`, tampil read-only di detail stok (Full per-batch DITUNDA). **(B)** Marketplace ULTG = **perkuat Reservasi TUG-5 sourceType=ULTG yg sudah ~80% ada** (`adoptTUG5ULTG` `useTugApprovals.js:296-330`); tambah jembatan reaktif "siap diambil" saat TUG-3 approve + browse-UI request. **(2)** Notif TUG-8/9 FINAL_APPROVED ke akuntansi via WA+Telegram (tabel `notif_outbox` + trigger + EF `notify-dispatch` + pg_cron, panel penerima tiru `TelegramWhitelistPanel.jsx`). **BLOCKER poin 2: pilih provider WA** (Meta Cloud API resmi vs tak-resmi) sebelum coding. Telegram existing tetap jalan.
